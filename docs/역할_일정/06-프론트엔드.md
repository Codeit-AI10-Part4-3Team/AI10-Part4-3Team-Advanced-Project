# 06. 프론트엔드 — 일정

> 역할 정의는 [역할_가이드/06-프론트엔드.md](../역할_가이드/06-프론트엔드.md)에 있습니다.
> **전체 일정과 어긋나면 [00-overall.md](00-overall.md)가 맞습니다.**

## 작성 규칙

- 날짜는 **절대 표기**(`2026-08-10`). "다음 주"는 나중에 읽을 수 없습니다.
- 각 작업에 **완료 조건(DoD)**을 적으세요. "구현한다"는 완료 조건이 아닙니다.
- 일요일과 공휴일(8/15, 8/17)은 작업일이 아닙니다.

## 작업

> 이 일정은 **2026-08-21에 확정되었습니다** ([일정 확정 회의록](../회의록/2026-08-21_일정_확정.md)).
> 제출 마감은 학원 공지로 **08-30(일)**, 실질 마감은 08-29(토)입니다.
> `S7`과 `S8`이 08-14로 당겨졌던 사정(이력)은 [00-overall.md](00-overall.md) 4-a절에 있습니다.
>
> **이 역할에 화면 작업이 몰려 있습니다** (00-overall 4-b절). 밀리면 날짜를 옮기지 말고
> 축소 순서([00-overall.md](00-overall.md) 7절 - Grid Post 확인, F10 세분화가 1 ~ 2순위)를
> 먼저 적용하세요.

**시작 전에 `apps/frontend/README.md`의 함정과 루트 `.gitignore`의 `lib/` 패턴을 먼저 읽으세요.**
무시된 디렉토리 안 파일은 git이 살려내지 못하므로, 스캐폴딩 결과가 조용히 사라질 수 있습니다.

| 작업 | 시작 | 완료 목표 | 완료 조건(DoD) | 상태 |
|---|---|---|---|---|
| `apps/frontend/` 스캐폴딩 | 08-12 | 08-13 | 빌드가 돌고 `frontend-ci.yml`이 자동 활성화됨 (non-required 유지). **같은 PR에서 그 워크플로의 `if: hashFiles(...)` 가드와 `--if-present`를 제거** — 남겨 두면 스크립트가 사라진 뒤에도 잡이 조용히 초록으로 통과합니다 | **완료 (08-13, #86)** -- 가드와 `--if-present`가 워크플로에 없습니다 |
| 로그인 화면 | 08-13 | 08-14 | 고정 계정으로 로그인, 로그아웃. 미인증 시 접근 차단 | **완료 (08-15)** -- 아래 참고 |
| S7 최소 화면 | 08-13 | **08-14** | 입력, 시안, 확정, 결과 다운로드가 브라우저에서 한 번에 통과 | **완료 (08-19)** -- 아래 참고 |
| S8 오류 표시 | 08-14 | **08-14** | ai-engine을 꺼도 화면이 죽지 않고 우측 상단에 오류가 보임 | **완료 (08-19)** -- 표시 위치는 상단 배너입니다 (아래 참고) |
| F3 제품 정보 입력 | ~~08-20~~ 08-21 | ~~08-21~~ **08-22** | 사진 1장 + 제품명 + 소구점 + 자유 메모를 한 화면에서 입력 | 대기 |
| F5 자동 채운 값 표시와 수정 | ~~08-21~~ 08-22 | ~~08-22~~ ~~08-24~~ **08-21** | `filledBy`가 화면에 드러나고 `editable` 항목을 고칠 수 있음. `needsInput` 세션의 재추론(`note` 채움 -> 재시도, B-11)이 화면에서 진행 가능 | **완료 (08-21, #166)** -- 목표보다 사흘 이릅니다 |
| 화풍 예시 8종 (03 공동) | 08-22 | **08-24** | 8종 각 1장 이상, 실제 결과물과 같은 프롬프트 조각 (A-3의 남은 절반). **F2와 검증 5순위의 선행 조건** | 대기 (**크리티컬 패스** - 00-overall 6절) |
| F1 출력 유형 선택 | ~~08-22~~ 08-24 | 08-24 | 만화형과 단일 광고형을 예시 이미지가 붙은 카드로 고름 | 대기 |
| 만화형 시안 화면 개방 | 08-24 | 08-25 | 만화형 세션에서 시안 생성이 가능하고 6칸 대사(`panels`)와 광고 기획안이 표시됨. 렌더 결과 3456 x 2304가 화면과 다운로드에서 동작 | 대기 (**개방 근거**: 실물 분기의 만화형이 #145, #150으로 완성되어 S7 때 "버튼을 두면 안 되는 이유"였던 거절이 실물 모드에서 소멸. ⚠️ **스텁 분기는 여전히 만화형을 거절합니다** - 로컬 개발은 실물 모드로 확인하거나 03과 스텁 확장을 먼저 합의) |
| F2 화풍 선택 | ~~08-24~~ 08-25 | ~~08-25~~ **08-26** | 후보를 그림 격자로 제시. 미선택 시 랜덤이 적용되고 그 사실이 보임 | 대기 (예시 08-24 + `ADGEN_ART_STYLES` 반영 08-25 전제) |
| 생성 대기 화면 | 08-25 | 08-25 | 진행률 없이 상태 문구 + 폴링. `Retry-After`를 따름 | 대기 |
| **열화 표기** | 08-26 | 08-26 | `messageMode: degraded` 세션이 일반 세션과 구분됨. `needsInput` 유무로 두 경우를 구분 | 대기 |
| F10 오류 표시 5종 | 08-26 | 08-27 | 기획서 12.5의 실패 5종이 각각 구분되어 표시됨 | 대기 |
| F13 면책 고지 | 08-27 | 08-27 | 결과 화면과 다운로드 안내에 문구가 표시됨 (문구는 01이 08-25까지 확정) | 대기 |
| Grid Post 게시 순서 확인 | 08-28 | 08-28 | 읽기 순서와 그리드 배치가 어긋나는지 실제 계정에서 확인 (18.2 #18) | 대기 (밀리면 축소 1순위) |

### 로그인 화면 완료와 다음 PR 범위 (2026-08-15)

**들어간 것**: React Router 도입, `/login` 화면, `GET /v1/me` 기반 세션 확인, 로그아웃,
보호된 경로(`RequireAuth`). 로컬에서 백엔드와 Vite 프록시를 함께 띄워 로그인 -> 조회 ->
로그아웃 -> 401 까지 확인했습니다.

**로그인을 먼저 붙인 이유는 선택이 아닙니다.** 계약의 보호 범위가 `/health` 와 `/v1/auth/*`
를 뺀 모든 `/v1` 경로입니다 (API_계약 6절). 세션 생성을 먼저 붙이면 첫 요청이 401 이고 그때
화면이 할 수 있는 일이 없습니다.

**CORS 는 다시 볼 필요가 없습니다.** #86 에서 Vite 프록시로 같은 출처를 만들어 두었고,
그것이 성립하는 조건은 `API_BASE_URL` 이 상대 경로로 남아 있는 것뿐입니다. 절대 URL 을 넣는
순간 프록시를 우회해 CORS 벽으로 되돌아갑니다.

#### 다음 PR: 실제 호출

| 순서 | 무엇 | DoD | 계약 |
|---|---|---|---|
| 1 | 세션 목록을 mock 에서 실물로 | 사이드바가 `mockSessions` 대신 내 세션을 보여 주고, 빈 목록이 오류가 아닌 정상 화면 | `GET /v1/sessions` |
| 2 | 세션 생성 | 사진 + 제품 정보가 multipart **한 번**으로 나가고 201 응답의 `state` 와 `needsInput` 유무로 세 갈래가 갈림 | `POST /v1/sessions` |
| 3 | 세션 조회와 브리프 표시 | 새로고침해도 `sessionId` 로 화면이 복원됨. `filledBy` 가 드러나고 `messageMode: degraded` 가 오류 화면으로 가지 않음 | `GET /v1/sessions/{id}` |
| 4 | 업로드 사진 표시 | `productImageUrl` 을 `<img src>` 에 그대로 싣고, 빈 문자열이면 그리지 않음 | N17 (API_계약 8.4절) |

**2번의 함정 셋**을 미리 적어 둡니다. 세션 생성은 실패가 아니라 **세 갈래**입니다 - 정상,
`needsInput` 이 붙은 정보 부족, `messageMode: degraded` 인 열화이고 셋 다 201 입니다. 이것을
성공/실패로만 나누면 뒤의 둘이 오류 화면으로 가고, 열화는 설계된 동작이라 사용자가 아무 잘못도
하지 않았는데 막힙니다 (ADR-0005).

**3번 이후는 `/sessions/:sessionId` 경로가 필요합니다.** 지금 라우터에 없습니다 - 화면이 없는
경로를 미리 만들지 않았기 때문이며, 그 화면을 만드는 PR 에서 함께 넣습니다.

### S7 · S8 완료 (2026-08-19)

위 "다음 PR" 네 항목에 시안 · 확정 · 렌더 · 다운로드를 더해 한 번에 붙였습니다. 소관은 06
(송기하)이고 이번 작업은 TL(신호정)이 대신 넣은 것입니다 - 관통이 08-14 실질 마감을 넘겨
[00-overall.md](00-overall.md) 5절의 두 마일스톤이 여기 하나에 걸려 있었기 때문입니다.
**소관이 바뀐 것이 아니므로 이후 프론트 작업(F1, F2, F3, F5, F10, F13)은 그대로 06입니다.**

**들어간 것**: `/sessions/:sessionId` 경로, 실제 세션 목록, multipart 세션 생성, 브리프 표시,
시안 생성, 확정, `Retry-After`를 따르는 잡 폴링, 결과 이미지 표시와 다운로드. `mock-data.ts`는
지웠습니다.

**관통 확인(2026-08-19, 로컬 스택)**: 로그인 -> 세션 생성(multipart) -> `brief_ready` ->
시안 생성 -> 확정 -> 잡 폴링(`queued`에 `Retry-After: 3`, `done`에는 없음) -> 결과 WebP 내려받기
-> 세션 `completed`. 미인증 요청은 401, ai-engine을 끈 상태의 세션 생성은 201 +
`brief_filling` + `messageMode: degraded`.

**남겨 둔 것과 그 이유**를 숨기지 않고 적습니다.

| 무엇 | 지금 화면 | 왜 여기서 멈췄나 |
|---|---|---|
| 브리프 수정 | `filledBy`만 표시하고 고칠 수 없음 | 고치는 쪽이 F5(08-21)입니다. 표시만 먼저 둔 이유는, 자동으로 채워진 값을 자기가 쓴 것으로 오해한 채 시안까지 가면 그 시점에는 브리프가 잠겨 되돌릴 수 없기 때문입니다 (INV-7) |
| `brief_filling` 세션 | 안내만 하고 진행 불가 | 진행하려면 브리프 수정이 필요하고, 그것이 F5입니다. 지금은 새 세션이 유일한 길이며 화면이 그렇게 말합니다 |
| 만화형 시안 | 세션은 만들어지고 시안 생성 버튼이 없음 | 관통 경로는 단일 광고형 하나이고 만화형은 분기만 둔 스텁입니다 ([구현_범위.md](../공통_가이드/구현_범위.md) 1절). ⚠️ **버튼을 두면 안 되는 이유가 따로 있습니다** - ai-engine이 만화형을 503으로 거절하고 backend가 그것을 `UPSTREAM_UNAVAILABLE`로 옮기므로, 화면에는 "엔진에 연결하지 못했습니다"로 도착합니다. 없는 장애를 있다고 말하는 화면이 됩니다 |
| 오류 표시 위치 | 헤더 아래 상단 배너 | S8의 DoD는 "우측 상단"이지만 토스트 컴포넌트를 새로 들이지 않았습니다. 실패 5종을 구분해 보여 주는 F10(08-26)에서 표시 방식을 한 번에 정하는 편이 낫습니다 |
| 화풍 선택 | `GET /v1/art-styles`가 비어 있어 "무작위 추천"만 | 후보가 아직 미정입니다 (미결정_대장 A절 3번). 화면에 그럴듯한 값을 적어 두면 그 값이 그대로 브리프에 저장되어, 진짜 후보가 정해진 뒤 어느 세션이 무엇으로 생성됐는지 알 수 없게 됩니다 |

## 의존 관계 (내가 막히면 누가 멈추는가 / 내가 기다리는 것)

| 방향 | 상대 역할 | 무엇 | 언제까지 |
|---|---|---|---|
| ~~막음~~ | 05, 07 | ~~S7 화면~~ — **08-19 완료.** 관통 확인과 E2E의 전제가 갖춰졌습니다 | 해소 |
| ~~기다림~~ | 02 | ~~`openapi.yaml` 교체~~ — **08-12 완료.** 붙일 계약이 생겼습니다 | 해소 |
| ~~기다림~~ | 05 | ~~S0 로그인과 S2 상태 전이~~ — **08-14 완료** | 해소 |
| **공동** | 03 | 화풍 예시 이미지 (A-3의 남은 절반. 목록은 08-19 확정). 없으면 F2를 만들 수 없습니다 | **08-24** |
| 기다림 | 05 | `ADGEN_ART_STYLES` 반영 배포. F2가 실제 값을 표시하는 전제입니다 | 08-25 |
| 기다림 | 01 | 면책 고지 문구 (미결정_대장 A절 7번). F13(08-27)의 선행 조건 | **08-25** |
