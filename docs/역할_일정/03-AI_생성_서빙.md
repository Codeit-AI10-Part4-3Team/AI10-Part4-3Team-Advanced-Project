# 03. AI 엔지니어 (생성·서빙) — 일정

> 역할 정의는 [역할_가이드/03-AI_생성_서빙.md](../역할_가이드/03-AI_생성_서빙.md)에 있습니다.
> **전체 일정과 어긋나면 [00-overall.md](00-overall.md)가 맞습니다.**

## 작성 규칙

- 날짜는 **절대 표기**(`2026-08-10`). "다음 주"는 나중에 읽을 수 없습니다.
- 각 작업에 **완료 조건(DoD)**을 적으세요. "구현한다"는 완료 조건이 아닙니다.
- 일요일과 공휴일(8/15, 8/17)은 작업일이 아닙니다.

## 작업

> 이 일정은 **2026-08-21에 확정되었습니다** ([일정 확정 회의록](../회의록/2026-08-21_일정_확정.md)).
> 제출 마감은 학원 공지로 **08-30(일)**, 실질 마감은 08-29(토)입니다.

> 2026-08-14 재배정으로 **02(테크리드)를 겸임하는 신호정이 03을 맡습니다**
> ([회의록](../회의록/2026-08-14_역할_재배정_임유빈_하차.md)). "새 날짜는 진행 보고 후
> 재조정한다"고 미뤄 두었던 것을 **2026-08-21 재산정으로 채웠습니다** - 아래 표의 미완료
> 행이 그 값입니다. 일상 작업 순서는 [notebooks/hj/WORKLIST.md](../../notebooks/hj/WORKLIST.md)
> (개인 사본, 근거 아님)에서 관리합니다.

**이 역할은 배관에 붙지 않습니다.** 더미 구현을 02가 맡기로 하면서 검증 트랙에 선행 조건이
없어졌습니다 ([구현_범위.md](../공통_가이드/구현_범위.md) 1.1절). 첫날부터 실험을 시작합니다.

검증 과제의 순위와 판정 기준은 기획서 15절이 정본이고, 담당은 2026-08-11 회의에서 확정되었습니다.

| 작업 | 시작 | 완료 목표 | 완료 조건(DoD) | 상태 |
|---|---|---|---|---|
| 검증 8순위 VM 스펙 확인 | 08-12 | ~~08-12~~ **08-18** | 로컬 모델 구동 가능 여부 확정 | **완료 (08-18)** -- 아래 참고 |
| **검증 1순위 한국어 렌더링** | 08-12 | 08-14 | 6칸 조건(3456 x 2304) 20회. 5칸 이상 오탈자 없는 비율이 80% 이상인지 판정. 예비안도 같은 회차에서 확인 | **완료 (08-14)** |
| 검증 6순위 비용 실측 | 08-13 | ~~08-13~~ **08-20** | 세션당 단가 숫자 | **완료 (08-20)** -- 08-14에 이미지 단가, 08-20에 텍스트 단가까지 실측 ([verify06_text_cost/RESULTS.md](../../notebooks/hj/verify06_text_cost/RESULTS.md)) |
| 검증 2순위 컷 경계 제어 | 08-18 | 08-19 | 6칸 균등 분할 여부와 **실패율**. 후처리 합성 도입 판단 근거 (미결정_대장 A-2) | **완료 (08-18)** -- 아래 참고 |
| **컷별 생성 + 3x2 합성 구현** | 08-20 | 08-20 | `image:render` 만화형이 칸 6장을 생성해 합성. 칸 규격 1152px 정확, 여백 0 (생성_파이프라인 6절) | **완료 (08-20, #150)** -- 운영 경로 실측 `low` 52.8초 / `medium` 118.0초 |
| **컷 병렬 호출과 실패 처리** | 08-20 | 08-20 | 2 ~ 6번 칸 병렬. 한 칸이라도 실패하면 잡 전체 `failed` (구현_범위 F9a, 미결정_대장 N20). **잡끼리의 직렬 1건은 05 소관** | **완료 (08-20, #150)** -- 타임아웃 예산은 #153, 값은 08-21 회의가 추인 ([회의록](../회의록/2026-08-21_미결정_5건_확정.md) 04) |
| **품질 티어 유형별 고정** | 08-20 | 08-20 | 만화형 `medium`, 단일 광고형 `low`. 설정 항목이 단일 값이라 유형별 분리가 가능한지 확인 포함 (생성_파이프라인 6.2절) | **완료 (08-20)** -- 아래 참고 |
| 검증 1순위 후속: 대사 길이 상한 시험 | 08-20 | 08-20 | 15 ~ 25자 회차(`--variant mid`)로 빈 구간 표본 확보 (미결정_대장 N18) | **완료 (08-20, #146)** -- 운영 조건 15 ~ 25자 무오탈자, **26자 이상 미측정**. 상한은 08-21 회의가 **프롬프트 지침 25자, 코드 검사 없음**으로 확정 |
| ~~B4 대사 길이 상한 분기~~ | - | - | ~~상한 초과 카피가 `draft:generate` 검사에 걸림~~ | **보류 (08-21 회의)** -- 강제 검사 코드를 만들지 않기로 확정 ([회의록](../회의록/2026-08-21_미결정_5건_확정.md) 01). 대체 작업은 아래 행 |
| N18 프롬프트 25자 지침 반영 | 08-22 | ~~08-22~~ **08-21** | `draft:generate` 프롬프트에 대사 25자 지침 문구가 들어감. 검사 코드는 만들지 않음(회의 결정) | **완료 (08-21, #177)** -- 값은 `draft_prompt.DIALOGUE_LENGTH_HINT` 하나이고 고치면 `VERSION` 도 함께 올립니다 |
| 검증 3순위 판정 회수와 집계 | 08-21 | ~~08-22~~ **08-21** | 조립된 12세트 배치를 판정자 3명에게 발송, 회수 후 `score_sheet.py tally` 집계. 판정자 3명 중 2명 이상 동일 인물(B-16). 장면 상태 축은 별도 집계 | **완료 (08-21)** -- 동일 인물 12/12 만장일치(기준 충족), 장면 상태 연속 10/12(참고 축). **수치 정본의 `docs/보고서/` 반영과 집계 코드 결함 2건 수정이 후속** |
| 검증 4순위 로컬 모델과 GPT 비교 | 08-20 | 08-21 | 채택 여부를 판단할 수 있는 비교 결과 | **완료 (08-20)** -- 미채택 확정 ([회의록](../회의록/2026-08-20_검증4순위_로컬모델_미채택.md)). 수행자 표기 정정이 남았습니다 (B-15) |
| 화풍 예시 8종 자체 생성 | 08-22 | **08-24** | 8종 각 1장 이상, **실제 결과물과 같은 프롬프트 조각**. 06과 공동 소관(A-3). 커밋 금지 - 공유 경로로 전달하고 `ADGEN_ART_STYLES` 반영은 05(08-25) | **생성 08-21, 판정 08-22 완료** -- 일정보다 이틀 당겼습니다. 프롬프트는 `render_prompt.build_panel` 을 그대로 호출해 조건을 코드로 고정했습니다. **`artStyleId` 에 특징을 일괄로 싣지 않습니다** -- 판정자 3명 집계가 특징 포함 1, 이름만 3, 둘 다 4 라 일괄로 붙일 근거가 없고, 1번만 만장일치로 특징 포함입니다. 게시 가능은 8종 전부 3/3. **8번은 두 조건 다 실사에 가까워 5순위에서 따로 봅니다**(이름에 맞는지는 판정자가 통과시켰고, 만화형으로 적절한지가 5순위 물음입니다). 기록: [verify05_art_styles/RESULTS.md](../../notebooks/hj/verify05_art_styles/RESULTS.md). **03 몫은 08-22 에 전부 끝났습니다** -- 잠정안대로 05 가 같은 날 답을 주었고(값과 서빙 경로), 전달본(WebP 8장 + `handoff.csv` + 설정 JSON 두 벌)도 공유했습니다. ~~**여기서 확정된 것은 없습니다** -- A-3 은 회의록이 있어야 닫힙니다.~~ **A-3 은 2026-08-24 회의가 확정했습니다** ([회의록](../회의록/2026-08-24_제출_전_8건_확정.md) 안건 01) -- 잠정안 그대로 추인이라 `name` 8종과 "`artStyleId` 는 1번만 특징 포함"이 그대로 확정값입니다. 남은 것은 05 의 `ADGEN_ART_STYLES` 반영과 볼륨 적재(#208)입니다. 판정이 닫지 못한 셋(8종이 서로 구분되는가 / 이름이 화풍을 가리키는가 / 진행 회고)은 의견 수렴 배치로 따로 넘겼고 **08-24 에 3/3 회수했습니다** -- 86px 격자에서 안 갈리는 짝은 6번과 8번 하나뿐이고 세 분이 전원 서로를 지목했습니다(C3 판정에서 눈으로 읽을 짝. **C3 는 08-24 에 "갈린다" 로 나왔고 조건이 다릅니다** -- 아래 행). 6번은 3/3 이 이름만으로 화풍을 못 떠올렸고(2번은 2/3), 이것이 A-3 회의 안건의 준비물이었습니다. 대안으로 나온 `실사 일러스트` 는 **회의가 미채택으로 정했습니다** -- 두 절을 함께 올린 결과가 아래 "알려진 한계 3건" 절입니다. 집계: [RESULTS.md](../../notebooks/hj/verify05_art_styles/RESULTS.md) |
| 검증 5순위 화풍 반영 | ~~08-21~~ ~~08-25~~ **08-22** | ~~08-22~~ ~~08-26~~ **08-24** | 화풍별 결과가 구분 가능함 (판정자 정승호, 임동규, 송기하 - 3명 중 2명 이상) | **완료 (08-24)** -- 도구 둘(판정 시트 `--task style`, 회차 하네스)을 갖추고 `low` 8종 48회를 돌렸습니다. **성공 8/8, 실패 0건, 0.78 USD.** 판정은 08-24 에 3/3 회수했고 **8회차 전부 3/3 `반영`, 구분되지 않는 쌍 지적 0건**이라 B-16 기준을 넘습니다. 일정보다 이틀 당겼습니다. 수치 정본: [검증5순위_화풍반영_판정_보고서.md](../보고서/검증5순위_화풍반영_판정_보고서.md). **주의: 6번과 8번이 여기서는 갈립니다** -- C4 의견 수렴이 86px 격자에서 "3/3 전원 서로 지목" 으로 닫은 짝인데 C3 는 1152px 6칸이고 셋 다 지적이 없습니다. 뒤집힌 것이 아니라 조건이 다르며, **사용자가 고르는 자리는 86px 카드라 두 수치를 함께 회의에 올려야 합니다.** 그리고 **8번의 "만화형으로 적절한가" 는 닫히지 않았습니다** -- 시트의 채점 열이 "지정 화풍으로 보이는가" 하나입니다. 두 건 다 08-24 회의에 올렸고 **알려진 한계 2, 3** 으로 남았습니다 (아래 절) |
| **`draft:patch` 라우트 신설** | 08-15 | 08-15 | ai-engine 이 `POST /v1/draft:patch` 를 서빙하고, 지정한 필드만 바뀐 시안이 돌아옴. 스텁 분기 | **완료 (08-15)** -- 아래 참고 |
| `brief:fill` 실물화 | 08-20 | 08-21 | 사진 판독 1회로 카테고리와 타겟이 채워짐 | **완료 (08-20, #145)** |
| `draft:generate` 실물화 | 08-21 | 08-22 | 시안이 실제 모델에서 나옴 (만화형 6칸 포함). `draft:patch` 실물 분기 포함 | **완료 (08-20, #145)** |
| `image:render` 실물화 | ~~08-22~~ **08-15** | ~~08-26~~ **08-15** | 단일 광고형 1088 x 1088. 만화형은 검증 결과에 따름 | **완료** (단일 08-15, 만화형 컷별은 08-20 #150) |
| 가드레일 이식 (F11) | 08-24 | ~~08-26~~ **08-20** | 프롬프트 지시 + 출력 검증(`guardrail.check_claims`) + 1회 재생성 후 `refusalReason: guardrail` | **완료 (08-20, #151, [ADR-0019](../adr/0019-광고_카피_가드레일은_금지_표현을_검출한다.md))** |
| **D2 절대값 보고 정리 (F12)** | **08-24** | **08-24** | 델타 대신 **절대값**("몇 건 중 몇 건 위반")으로 집계 정리 + [생성_파이프라인.md](../기술문서/생성_파이프라인.md) 5.3절 보고 지표 문안 수정. 표본 한정(위반 유도 예문 4종, 6회차) 명기. 확인(07)은 08-26 | 대기 (**08-21 회의 확정** -- [회의록](../회의록/2026-08-21_미결정_5건_확정.md) 03. 재실험 없이 이번 회차로 충분해져 이틀에서 하루로 줄임) |
| ai-engine `/v1/generate` 라우트 삭제 | 08-20 | ~~08-25~~ **08-21** | 가드레일 이식 완료로 라우트 수준 사용처가 소멸. `verify` · `GuardrailContext` 와 함께 제거 | **완료 (08-21, #163)** -- ai-engine 이 서빙하는 경로가 전부 계약에 있게 됐습니다 |
| 지표 실측 | ~~08-26~~ **08-25** | ~~08-27~~ **08-25** | `eval/` 하네스 출력이 문서의 목표치 자리를 대체 (확인은 07, 08-27 ~ 28) | **완료 (08-25, #255)** -- 일정보다 이틀 당겼습니다. 지표 표 다섯 행 중 **넷을 실측으로 채웠습니다** -- 생성 지연 p50 24.05초 / p95 27.05초(단일 광고형 `low`, 로컬 스택, 표본 20), 카피 사실 일치율 on 1.0 / off 0.9268(주장 39 / 41), 가드레일 위반 건수 **양 팔 0건**(표본 26 / 26), 열화 발생률 0.35. 뒤의 둘은 그대로 인용하면 안 됩니다 -- 표본 26 의 0건은 실제 위반율 약 11퍼센트 이하까지만 배제하고, 열화 0.35 는 벤더의 그날 속도와 브리프 한 종류에 걸린 조건 한정값입니다. 수치 정본: [지표_실측_보고서.md](../보고서/지표_실측_보고서.md). **남은 한 행("브랜드 스타일 일치도")은 이 회차가 채우지 않았습니다** -- 브랜드 레퍼런스셋이 없어 막혀 있었고, 08-25 회의가 **04 지지제품/상황 부합도로 교체**하기로 확정해 별도 과제로 나갔습니다 (N21, [회의록](../회의록/2026-08-25_제출_전_6건_확정.md) 안건 01. 구현은 04 소관이고 03 은 지원, 이슈 #258). 행을 순번으로 부르지 마세요 -- 회의록은 "다섯째 행"으로 적었지만 개발자_가이드 4절과 `run_metrics.py` 의 `_TABLE` 에서는 둘째 행입니다. **목표(가설) 칸은 다섯 행 모두 여전히 TBD 입니다** -- 이 회차는 실측값을 낼 뿐 목표 달성 여부를 판정하지 않습니다. 확인(07)은 08-27 ~ 28 그대로입니다 |
| L4 타당성 검토서 커밋 (E3) | 08-20 | 08-21 | 08-20 회의록이 근거로 삼은 문서가 `docs/보고서/`에 존재하고 문자 규약 통과 | **완료 (08-21, #159)** -- [로컬모델_L4_타당성_검토서.md](../보고서/로컬모델_L4_타당성_검토서.md). 문자 규약 위반 90건 치환 완료 |
| 생성_파이프라인 비용 표 갱신 (E5) | 08-20 | 08-20 | 텍스트 단가 반영, "가장 비싼 4단계" 서술 정정 | **완료 (08-20)** -- #151 에 포함되어 이미 머지됨. 1절 표에 반영 확인 |
| 인프라 문서 보완 (E1) | ~~08-22~~ 08-20 | ~~08-22~~ 08-21 | 실제 사용 모델과 하드웨어 최적화 여부를 문서에 추가 (08-11 회의) | **완료 (08-21, #164)** -- [infra/README.md](../../infra/README.md)에 "이 VM에서 실제로 도는 것" 절 추가 |

**검증 결과 이미지는 그때그때 메신저에 올립니다.** 판정자가 결과를 못 보면 교차 검증이 형식만
남습니다. 판정은 AI 담당을 제외한 전원이 합니다 -- **"AI 담당"은 03만 가리킵니다**
(2026-08-14 확정, [회의록](../회의록/2026-08-14_역할_재배정_임유빈_하차.md)).

### 소관 배정 4건과 화풍 후보 8종 확정 (2026-08-19)

근거: [2026-08-19 회의록](../회의록/2026-08-19_소관배정_화풍확정_카피경로확인.md). 08-18 회의가
방향만 정하고 담당을 비워 둔 항목 넷이 이 회의로 배정되었습니다.

| 항목 | 소관 |
|---|---|
| 컷별 생성 구현 (칸 6장 생성 + 3x2 합성) | **03 (신호정)** |
| 병렬 호출과 부분 실패 처리 (ai-engine 안에서 5건 동시 호출) | **03 (신호정)** |
| 품질 티어의 출력 유형별 분리 적용 | **03 (신호정)** |
| 대사 상한 추가 시험 (빈 구간 표본) | **03 (신호정)** |
| 잡 단위 동시 실행 수 상한 / 큐 처리 | **05 (임동규)** |

**마지막 행이 03에서 갈라진 이유**는 소유 경로입니다. `render_timeout_s`와 잡 워커가
`backend_core` 소유이므로 05가 맡습니다. 즉 **동시성이 두 곳에 생깁니다** -- 잡 하나 안에서
칸 5장을 동시에 부르는 것은 03, 잡끼리 몰리지 않게 막는 것은 05입니다.

⚠️ **두 담당이 인터페이스를 먼저 맞춰야 합니다.** 병렬과 실패 처리가 `apps/ai-engine`과
`apps/backend` 양쪽에 걸치는데, 두 앱의 허용된 결합은 `packages/contracts`의 HTTP 계약뿐입니다
([AGENTS.md](../../AGENTS.md) 아키텍처 경계). 구현을 먼저 시작하면 그 선을 넘는 쪽이 쉬워집니다.

**날짜는 비워 둡니다.** 회의가 소관만 정했고, 마일스톤 일자는 여전히 미확정입니다
([미결정_대장.md](../기술문서/미결정_대장.md) B-20).

> **해소 (2026-08-21).** 배정 4건 중 셋(컷별 생성, 병렬과 실패 처리, 품질 티어)은 08-20에
> 구현이 끝났고, 비워 두었던 날짜는 2026-08-21 재산정으로 채워 위 작업 표에 있습니다.

### 품질 티어는 요청이 실어 보냅니다 (2026-08-20)

배정 4건 중 "품질 티어의 출력 유형별 분리"를 닫았습니다. **설정을 쪼개는 대신 계약에 실었습니다.**

- 계약에 `ImageQuality`(`low` / `medium` / `high`)와 `ImageRenderRequest.quality`(**필수**)가
  생겼습니다. 값의 정본은 호출자인 backend 의 `COMIC_QUALITY` / `SINGLE_AD_QUALITY` 상수입니다.
- **엔진이 `outputType` 에서 유도하지 않는 이유는 `spec` 과 같습니다.** 같은 계약이 이미
  "호출자가 규격을 정해 보냅니다. 엔진이 유형을 보고 스스로 정하면 값이 두 곳에 생기고, 한쪽만
  고치는 순간 어긋납니다"라고 못박아 두었고, 티어도 유형별로 갈리는 값이라 사정이 같습니다.
- **벤더의 `auto` 는 열거에 넣지 않았습니다.** 모델에게 맡기면 같은 요청의 비용이 최대 9배까지
  흔들립니다 (2026-08-13, 08-15 실측). `high` 는 열거에 있지만 재지 않았습니다 -- 쓰기로 하면
  병렬 소요 시간과 타임아웃을 다시 재야 합니다 (N19-a).
- **`ADGEN_IMAGE_QUALITY` 는 `ADGEN_IMAGE_QUALITY_OVERRIDE` 로 이름이 바뀌었습니다.** 개발과
  검증 실험을 `low` 로 돌리기 위한 스위치입니다 -- 그러자고 backend 의 운영 상수를 고치면 그
  값이 그대로 배포됩니다. 발동하면 경고 로그가 남으며, **그 상태로 잰 숫자는 운영 경로의 숫자가
  아닙니다.**

**남은 것 하나**: `infra/.env.example` 의 항목 이름 갱신입니다 (소관 05, 미결정_대장 E-2).
`docker-compose.yml` 은 이번에 함께 고쳤습니다.

#### 화풍 후보 8종 (미결정_대장 A-3 확정)

같은 회의가 A-3을 닫았습니다. 8종이라는 숫자의 근거는 기획서 12.2의 4 x 2 화면 격자이고,
그 안에 들어갈 목록이 이번에 정해졌습니다. **이 표가 목록의 정본입니다.**

| # | 화풍 | 특징 | 잘 맞는 광고 |
|---|---|---|---|
| 1 | 심플 플랫 웹툰 | 굵은 선, 단순한 색, 풍부한 표정 | 앱, 플랫폼, 생활 서비스 |
| 2 | 한국형 일상 웹툰 | 친근한 인물과 현실적인 공감 상황 | 식품, 배달, 금융, 교육 |
| 3 | SD 캐릭터, 치비 | 큰 머리와 과장된 리액션, 귀여운 인상 | 캐릭터 상품, 게임, 젊은 타깃 |
| 4 | 레트로 팝아트 | 강한 원색, 망점, 광고 포스터 같은 연출 | 패션, 음료, 이벤트 |
| 5 | 감성 수채화 | 부드러운 색과 손으로 그린 듯한 질감 | 여행, 카페, 뷰티, 친환경 제품 |
| 6 | 세련된 에디토리얼 | 절제된 선과 색, 잡지 일러스트 분위기 | 프리미엄 브랜드, 금융, B2B |
| 7 | 3D 클레이 캐릭터 | 말랑하고 입체적인 캐릭터, 높은 주목도 | IT 서비스, 키즈, 프로모션 |
| 8 | 시네마틱 애니메이션 | 극적인 구도와 조명, 이야기 몰입도가 높음 | 자동차, 게임, 브랜드 캠페인 |

> **확정 (2026-08-24).** A-3 이 회의록으로 닫혔습니다
> ([제출 전 8건 확정 회의록](../회의록/2026-08-24_제출_전_8건_확정.md) 안건 01). 회의는
> **잠정안을 그대로 추인**했습니다 -- 위 표의 `화풍` 열 8종이 확정된 `name` 이고,
> `artStyleId` 는 **1번만 특징을 포함하고 나머지 일곱은 이름만 싣습니다**. 판정자 3명 집계가
> 특징 포함 1, 이름만 3, 둘 다 4 로 갈려 일괄로 붙일 근거가 없고 1번만 만장일치였기
> 때문입니다. 값이 코드로 들어가는 통로는 여전히 `ADGEN_ART_STYLES` 설정 하나이며(아래),
> 반영은 05 몫입니다. **확정과 함께 알려진 한계 3건이 남았습니다 -- 바로 다음 절입니다.**

**목록이 정해졌다고 이 항목이 끝난 것은 아닙니다.** 남은 것 둘입니다.

- **예시 이미지를 자체 생성**해야 합니다. 외부 이미지를 가져오면 권리 확인이 필요하고 저장소가
  public이라 커밋되는 순간 되돌릴 수 없습니다. **예시와 실제 결과물의 화풍이 같은 프롬프트
  조각에서 나와야** 합니다 -- 예시만 예쁘고 결과가 다르면 선택 UI 전체가 거짓말이 됩니다.
- 값이 코드로 들어가는 통로는 `ADGEN_ART_STYLES` 설정뿐입니다. 코드에 박지 마세요.

검증 5순위(화풍 반영) 판정 명단도 같은 회의에서 재확인되었습니다: **정승호, 임동규, 송기하**
(B-15 그대로, AI 담당인 신호정 제외).

#### 화풍 8종의 알려진 한계 3건 (2026-08-24)

근거: [제출 전 8건 확정 회의록](../회의록/2026-08-24_제출_전_8건_확정.md) 안건 01.
**A-3 은 확정됐지만 아래 셋은 확정으로 닫히지 않았습니다.** 회의가 적은 그대로 옮깁니다.

- **한계 1 -- 이름 인식.** 6번(세련된 에디토리얼)은 판정자 **3/3**, 2번(한국형 일상 웹툰)은
  **2/3** 이 이름만 보고 그림체를 떠올리지 못했습니다 (C4 의견 수렴, PR #221). 이름은 이번
  회의로 확정하되 이 실패율은 알려진 한계로 남깁니다. **6번의 대안 이름 후보
  `실사 일러스트` 는 미채택입니다** -- 아래 한계 2와 겹칠 위험이 있기 때문입니다.
- **한계 2 -- 구분 가능성은 조건에 따라 결과가 갈립니다.** C3(검증 5순위 판정, PR #222)는
  **1152px 6칸** 세트에서 8회차 전부 3/3 반영 통과에 구분 안 되는 쌍 지적 **0건**이고,
  C4(의견 수렴, PR #221)는 **86px 카드**에서 **6번과 8번이 3/3 헷갈림**입니다. **뒤집힌 것이
  아니라 조건이 다릅니다** -- 6칸이면 표정과 배경 단서가 여러 번 나오고 크기도 13배
  차이입니다. 그리고 **사용자가 실제로 화풍을 고르는 화면은 86px 카드 쪽**이라, C3 의 통과를
  "6번, 8번 구분 문제가 해결됐다"로 읽지 않습니다. 86px 조건의 실측은 여전히 없습니다.
- **한계 3 -- 8번의 만화형 적합성은 C3 로도 안 닫힙니다.** C3 판정 시트의 채점 열이 "지정
  화풍으로 보이는가" 하나뿐이라, 8번의 통과는 "시네마틱 애니메이션처럼 보인다"는 뜻이지
  "만화형 광고 소재로 적절하다"는 뜻이 아닙니다 (정본:
  [검증5순위_화풍반영_판정_보고서.md](../보고서/검증5순위_화풍반영_판정_보고서.md) C-c).

**셋 중 하나라도 실제 사용에서 문제가 되어 이름 재검토가 필요해지면 다음 회의에서 셋을 함께
다룹니다** -- 하나씩 따로 고치면 같은 문제를 반복합니다.

### 인수 항목: `draft:patch` 누락 (2026-08-15 해소)

재배정으로 03 을 인수하면서 **계약에는 있는데 엔진이 서빙하지 않는 경로**가 하나 넘어왔습니다.
`apps/ai-engine` 이 `/health` `/v1/generate` `/v1/brief:fill` `/v1/draft:generate`
`/v1/image:render` 다섯만 서빙하고 있었고 `/v1/draft:patch` 가 없었습니다.

**증상이 엉뚱한 곳을 가리킵니다.** backend 클라이언트는 그 경로를 부르므로 실제 엔진 상대로는
404 가 돌아오고, 404 는 다른 HTTP 실패와 같이 매핑되어 사용자에게 **`503 UPSTREAM_UNAVAILABLE`**
로 나갑니다. 즉 S5(부분 교체)가 죽어 있는데 화면에는 "모델 API 를 쓸 수 없습니다"라고 뜨고,
디버깅하는 사람은 멀쩡히 돌고 있는 서비스를 들여다보게 됩니다. **S5 는 테스트 fake 상대로만
동작하던 상태**였습니다.

스텁 분기로 닫았습니다. 실물 분기(`_patch_with_model`)는 `draft:generate` 실물화와 같은
시점입니다 -- 두 경로가 같은 모델 호출을 공유하므로 따로 잡지 않았습니다.

## 검증 1순위 · 6순위 결과 (2026-08-14)

전량 기록: [RESULTS.md](../../notebooks/hj/verify01_korean_text_rendering/RESULTS.md).
하네스와 판정 시트는 [notebooks/hj/verify01_korean_text_rendering/](../../notebooks/hj/verify01_korean_text_rendering/).

- **1순위 통과.** 본안 20회에서 판정자 3명 전원 100%(기준선 80%), 예비안 10회도 100%.
  다만 **두 안이 정확도로 갈리지 않아 A-1은 아직 닫히지 않았습니다** -- 선택 축이 비용과
  수정 가능성으로 옮겨갔고, 확정은 회의록 사안입니다.
- **6순위는 절반이었습니다** (08-14 시점). 이미지 1회 단가는 실측했지만($0.1171, 만화형),
  세션은 `brief:fill` + `draft:generate` + `image:render` 세 호출이고 앞의 둘은 **카피 생성
  경로가 TBD**라 잴 대상이 없었습니다.

  **나머지 절반은 2026-08-20에 닫혔습니다** (PR #151, 전량 기록은
  [verify06_text_cost/RESULTS.md](../../notebooks/hj/verify06_text_cost/RESULTS.md)).
  08-19에 카피 경로가 확정되고(기획서 13.2 유지) 08-20에 실물 분기가 배선되면서 잴 대상이
  생겼습니다. 세션당 텍스트 비용은 **단일 광고형 0.0277 USD, 만화형 0.0305 USD**이고 부분
  교체는 1회마다 0.0090 USD입니다.

  주의: **PM에게 넘길 확정치가 "이미지뿐"이 아니게 됐습니다.** 단일 광고형은 텍스트가
  이미지보다 4배 비싸(0.0277 대 0.0069) 세션 비용의 80%가 텍스트입니다. 예산 상한을 이미지
  기준으로만 잡으면 그 유형에서 틀립니다.
- **2순위에 쓸 데이터가 함께 나왔습니다.** 같은 이미지 안의 6칸 균등 분할은 30건 전부 성공했고,
  실제 문제는 **이미지 간 여백 편차**로 드러났습니다 (미결정_대장 A-2). 08-18 실험은 이 축으로
  다시 설계해야 합니다.
- **부수 실측 두 가지**: 생성 시간 54 ~ 122초(동기 HTTP 금지 결정의 근거), `quality` 미지정 시
  품질 티어가 회차마다 흔들려 비용이 2.3배까지 벌어짐(예산 상한은 높은 티어 기준으로).

> **"AI 담당"은 03(생성·서빙)만 가리킵니다** (2026-08-14 확정). 이 역할이 수행한 1, 3, 5순위의
> 판정자는 **정승호·임동규·송기하 3명이고 2명 이상 동의로 통과**합니다. 4순위는 수행자가 04라
> 판정자가 임동규·송기하 2명이며 두 사람 모두 동의해야 합니다
> ([미결정_대장.md](../기술문서/미결정_대장.md) B-15, B-16).

### `image:render` 실물화를 앞당긴 이유 (2026-08-15)

**일정표와 판정 기준이 어긋나 있었습니다.** 중간 점검(08-17)의 PM 확정 문구는 "워킹 스켈레톤이
관통하고, **외부 이미지 생성 API 호출이 그 경로를 지나 실제 이미지를 반환할 것**"인데
([00-overall.md](00-overall.md) 3-b절), 이 표의 `image:render` 실물화는 08-22 ~ 08-26 이었습니다.
판정일보다 뒤입니다. 순서를 바꿔 먼저 배선했습니다.

**앞당길 수 있었던 이유는 검증 1순위가 이미 그 호출을 통과시켰기 때문입니다.** 모델, 규격,
응답 형태, 소요 시간, 단가가 전부 실측돼 있어(RESULTS.md) 남은 일은 하네스의 호출 모양을
이음매로 옮기는 것이었습니다. 프롬프트 문장도 실험이 쓴 것과 같은 뼈대를 씁니다 -- 다르면
20회를 들여 잰 결과가 지금 서비스가 쓰지 않는 문장의 결과가 됩니다.

**여전히 기본값은 스텁입니다.** `ADGEN_GENERATION_MODE=stub` 이 기본이고, 켜는 쪽이 위험하기
때문입니다 -- 켜면 요청마다 요금이 나갑니다. 실물로 돌릴 때는 키를 함께 채우고, 키 없이 켜면
렌더가 503 으로 명시적으로 실패합니다 (스텁으로 되돌아가지 않습니다).

**남은 실물화는 `brief:fill` 과 `draft:generate` 둘입니다.** 08-20 ~ 08-22 그대로입니다.

> **해소 (2026-08-20).** 둘 다 #145로 실물화되었고, `draft:patch` 실물 분기도 같은 PR에
> 들어갔습니다. 만화형 컷별 생성은 #150, 가드레일은 #151입니다.

## 검증 2순위 결과와 A-1, A-2 확정 (2026-08-18)

실측 근거는 [API생성_시험_보고서.md](../보고서/API생성_시험_보고서.md) E절,
[로컬모델_탐색_보고서.md](../보고서/로컬모델_탐색_보고서.md),
[생성_경로_비교표.md](../보고서/생성_경로_비교표.md)입니다. 확정 근거는
[회의록](../회의록/2026-08-18_이미지_생성_경로_결정.md)입니다 -- 보고서는 수치를 제공할 뿐
결정이 아닙니다 ([AGENTS.md](../../AGENTS.md) 근거 자료 규칙).

- **A-2 확정. 컷별 생성 후 3x2 합성이 기본 경로입니다.** 08-14 시점에 "조건부 후처리"였던 것이
  기본 경로로 올라갔습니다. 전제 두 개가 실측으로 뒤집혔기 때문입니다 -- 호출 수가 6배인데도
  `low` 기준 **17% 저렴**하고(0.0974 대 0.1171 USD), 병렬 호출로 **55.9초**라 한 장 생성
  (54 ~ 122초)과 같은 구간입니다.
- **08-14에 적어 둔 "여백 편차로 실패 기준을 다시 설계한다"는 과제는 소멸했습니다.** 재려던
  대상이 컷별 생성에서는 존재하지 않습니다. 여백을 합성 단계가 결정하므로 편차가 0입니다.
- **A-1 확정. 본안 유지이고 예비안은 미채택입니다.** 정확도로는 갈리지 않았지만(둘 다 100%),
  예비안에는 **말풍선 위치를 사후 검출하는 단계**가 붙는데 그것이 된다는 증거가 없습니다.
  직선인 격자 경계조차 14 ~ 36px 어긋난 마당에 곡선은 더 어렵습니다.
- **품질 티어를 유형별로 고정했습니다.** 만화형 `medium`, 단일 광고형 `low`. `low` 만화형에서
  손과 사물의 물리가 무너지는 결함이 관측되었습니다.

**여기서 세 항목이 새로 열렸습니다** (미결정_대장 N18, N19, N20). 위 표의 "배정 대기" 네 줄이
그 항목들이며, **회의가 담당자를 정하지 않았습니다.** 컷별 생성 구현과 같은 사람이 맡아야 합니다.

**`medium` 병렬도 실측으로 채웠습니다** (08-18 추가 검증). 회의 시점에는 `low` 만 재고 `medium`
은 같은 비율을 가정한 판단이었는데, 세 티어를 다 재면서 그 가정이 확인되었습니다.

| 티어 | 순차 | 병렬 | 단축 | 개별 호출 팽창 |
|---|---|---|---|---|
| `low` | 132.4초 | 55.9초 | 57.8% | 44% |
| `auto` | 203.6초 | 83.6초 | 58.9% | 18% |
| `medium` | 310.8초 | **139.2초** | 55.2% | 64% |

**이것으로 타임아웃 위험이 좁아졌습니다.** `medium` 병렬 139.2초는 `render_timeout_s`(300초) 안에
2배 이상 여유가 있습니다. 남은 위험은 **순차 구현이 먼저 나가는 경우**(310.8초) 하나이며, 그
순서로 간다면 타임아웃을 함께 올려야 합니다 (N19-a).

**주의 둘.** 표본이 티어당 1세트입니다. 그리고 동시에 보내면 개별 호출이 느려지는데 **무거운
티어일수록 팽창이 큽니다**(`medium` 64%). `high` 는 재지 않았고, 팽창이 커질수록 병렬의 이득이
줄어들므로 티어를 더 올리는 결정이 나오면 다시 재야 합니다.

### 검증 8순위도 같은 보고서로 닫혔습니다 (2026-08-18)

**표에서 빠뜨렸던 정정입니다.** 8순위(VM 스펙 확인, 완료 조건은 로컬 모델 구동 가능 여부)는
[로컬모델_탐색_보고서.md](../보고서/로컬모델_탐색_보고서.md) 1절이 **가능으로 닫았는데**
상태 열이 "대기"로 남아 있었습니다. 실험은 08-15, 보고서 작성은 08-18, 머지는 PR 116 입니다.

세 후보(FLUX.1-schnell, SDXL, Qwen-Image Q4) 모두 가용 VRAM 22.5GB 안에서 구동되었습니다.
**구동 가능 여부와 채택 여부는 다른 물음입니다** -- 채택은 검증 4순위 소관이고 A-5 는 열려
있습니다. 같은 항목이 [04 일정](04-AI_학습_데이터.md)에도 있어 함께 고쳤습니다.

## 의존 관계 (내가 막히면 누가 멈추는가 / 내가 기다리는 것)

| 방향 | 상대 역할 | 무엇 | 언제까지 |
|---|---|---|---|
| ~~막음~~ | 02, 05 | ~~검증 1, 2순위 결과~~ - **08-18 해소.** 이음매 실물화도 08-20에 끝났습니다 | 해소 |
| ~~막음~~ | 01 | ~~검증 6순위 비용 실측~~ - **08-20 해소.** 텍스트 단가까지 나왔고, 예산 상한 결정은 08-21 회의 안건입니다 | 해소 |
| **공동** | 06 | ~~화풍 예시 이미지 (A-3의 남은 절반)~~ -- **생성과 판정 완료 (08-22).** 남은 것은 `artStyleId` 값입니다. 판정 집계로 06과 낼 수 있는 것은 **잠정안까지이고, 확정에는 회의록이 필요했습니다** -- A-3은 미결정 대장 항목이라 소관자 합의로는 잠정안만 갱신됩니다([AGENTS.md](../../AGENTS.md) 근거 자료 규칙 2번). **그 회의록이 2026-08-24 에 나왔고 잠정안 그대로 추인되었습니다** ([회의록](../회의록/2026-08-24_제출_전_8건_확정.md) 안건 01). 잠정안 기록: [RESULTS.md](../../notebooks/hj/verify05_art_styles/RESULTS.md). **의견 수렴 배치를 08-22 에 공유하고 08-24 에 3/3 회수했습니다** -- 8종이 서로 구분되는지, 이름이 화풍을 가리키는지를 물었고, 그 답이 A-3 회의 안건의 준비물입니다. 6번은 3/3, 2번은 2/3 이 이름만으로 화풍을 못 떠올렸습니다. **06 과 함께 1절(6번과 8번이 안 갈림)과 2절(이름)을 같이 올렸습니다** -- 6번 대안으로 나온 `실사 일러스트` 가 8번 쪽으로 더 붙는 이름이라, 따로 올리면 그 충돌이 안 보입니다. 회의는 이름을 확정하고 대안은 미채택으로 정했으며, 두 절은 **알려진 한계 1, 2** 로 남았습니다. 집계: [RESULTS.md](../../notebooks/hj/verify05_art_styles/RESULTS.md) | ~~08-24~~ **해소 (08-24). 회의가 A-3 을 확정했고 남은 것은 05 의 설정 반영** |
| ~~막음~~ | 05 | ~~화풍 예시 확정본~~ -- **해소 (08-22).** 서빙은 backend 정적 마운트로 정해졌고(VM `adgen-state` 볼륨 `/data/art-styles/`, URL `/static/art-styles/<파일명>`), 전달본도 공유했습니다. `ADGEN_ART_STYLES` 반영과 볼륨 적재는 05 몫이고 #208 이 그 구현입니다. ⚠️ **마운트 대상이 `/data` 면 같은 볼륨의 `adgen.sqlite` 와 사용자 업로드 사진이 인증 없이 열립니다** - 정적 마운트에는 라우트 의존성이 걸리지 않습니다 | 해소 |
| ~~기다림~~ | 01 | ~~08-21 회의 확정~~ -- **해소 (08-21).** 5건이 확정되어 N18은 프롬프트 지침으로, D2는 절대값 정리로 진행합니다 ([회의록](../회의록/2026-08-21_미결정_5건_확정.md)) | 해소 |
| ~~기다림~~ | 01 | ~~B-20(일정) 확정 회의록~~ -- **08-21 해소** ([일정 확정 회의록](../회의록/2026-08-21_일정_확정.md)) | 해소 |
| 기다림 | 전원 | 판정 (3순위 08-22, 5순위 08-26). 결과를 만든 사람이 채점하지 않습니다 | 상시 |
