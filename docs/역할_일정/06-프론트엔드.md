# 06. 프론트엔드 — 일정

> 역할 정의는 [역할_가이드/06-프론트엔드.md](../역할_가이드/06-프론트엔드.md)에 있습니다.
> **전체 일정과 어긋나면 [00-overall.md](00-overall.md)가 맞습니다.**

## 작성 규칙

- 날짜는 **절대 표기**(`2026-08-10`). "다음 주"는 나중에 읽을 수 없습니다.
- 각 작업에 **완료 조건(DoD)**을 적으세요. "구현한다"는 완료 조건이 아닙니다.
- 일요일과 공휴일(8/15, 8/17)은 작업일이 아닙니다.

## 작업

> TL 초안입니다. 확정은 회의록으로 합니다 ([00-overall.md](00-overall.md) 머리말).
> 단 스켈레톤 관통 일자(08-17, 실질 마감 08-14)는
> [2026-08-13 회의록](../회의록/2026-08-13_워킹스켈레톤_일정_조정.md)으로 확정되었습니다.
> `S7`과 `S8`이 08-14로 당겨진 사정은 [00-overall.md](00-overall.md) 4-a절에 있습니다.

**시작 전에 `apps/frontend/README.md`의 함정과 루트 `.gitignore`의 `lib/` 패턴을 먼저 읽으세요.**
무시된 디렉토리 안 파일은 git이 살려내지 못하므로, 스캐폴딩 결과가 조용히 사라질 수 있습니다.

| 작업 | 시작 | 완료 목표 | 완료 조건(DoD) | 상태 |
|---|---|---|---|---|
| `apps/frontend/` 스캐폴딩 | 08-12 | 08-13 | 빌드가 돌고 `frontend-ci.yml`이 자동 활성화됨 (non-required 유지). **같은 PR에서 그 워크플로의 `if: hashFiles(...)` 가드와 `--if-present`를 제거** — 남겨 두면 스크립트가 사라진 뒤에도 잡이 조용히 초록으로 통과합니다 | 대기 |
| 로그인 화면 | 08-13 | 08-14 | 고정 계정으로 로그인, 로그아웃. 미인증 시 접근 차단 | 대기 |
| S7 최소 화면 | 08-13 | **08-14** | 입력, 시안, 확정, 결과 다운로드가 브라우저에서 한 번에 통과 | 대기 |
| S8 오류 표시 | 08-14 | **08-14** | ai-engine을 꺼도 화면이 죽지 않고 우측 상단에 오류가 보임 | 대기 |
| F3 제품 정보 입력 | 08-20 | 08-21 | 사진 1장 + 제품명 + 소구점 + 자유 메모를 한 화면에서 입력 | 대기 |
| F5 자동 채운 값 표시와 수정 | 08-21 | 08-22 | `filledBy`가 화면에 드러나고 `editable` 항목을 고칠 수 있음 | 대기 |
| F1 출력 유형 선택 | 08-22 | 08-22 | 만화형과 단일 광고형을 예시 이미지가 붙은 카드로 고름 | 대기 |
| F2 화풍 선택 | 08-24 | 08-25 | 후보를 그림 격자로 제시. 미선택 시 랜덤이 적용되고 그 사실이 보임 | 대기 |
| 생성 대기 화면 | 08-25 | 08-25 | 진행률 없이 상태 문구 + 폴링. `Retry-After`를 따름 | 대기 |
| **열화 표기** | 08-26 | 08-26 | `messageMode: degraded` 세션이 일반 세션과 구분됨. `needsInput` 유무로 두 경우를 구분 | 대기 |
| F10 오류 표시 5종 | 08-26 | 08-27 | 기획서 12.5의 실패 5종이 각각 구분되어 표시됨 | 대기 |
| F13 면책 고지 | 08-27 | 08-27 | 결과 화면과 다운로드 안내에 문구가 표시됨 | 대기 |
| Grid Post 게시 순서 확인 | 08-28 | 08-28 | 읽기 순서와 그리드 배치가 어긋나는지 실제 계정에서 확인 (18.2 #18) | 대기 |

## 의존 관계 (내가 막히면 누가 멈추는가 / 내가 기다리는 것)

| 방향 | 상대 역할 | 무엇 | 언제까지 |
|---|---|---|---|
| **막음** | 05, 07 | S7 화면. 관통 확인과 E2E가 화면 없이는 성립하지 않습니다 | **08-14** |
| ~~기다림~~ | 02 | ~~`openapi.yaml` 교체~~ — **08-12 완료.** 붙일 계약이 생겼습니다 | 해소 |
| 기다림 | 05 | S0 로그인과 S2 상태 전이 | 08-14 |
| **공동** | 03 | 화풍 후보와 예시 이미지 (A-3 공동 소관). 목록이 없으면 F2를 만들 수 없습니다 | 08-20 |
| 기다림 | 01 | 면책 고지 문구 (미결정_대장 A절 7번) | 08-26 |
