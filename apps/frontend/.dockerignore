# 빌드 컨텍스트에서 뺄 것.
#
# ⚠️ `node_modules` 가 빠지는 것이 핵심입니다. 호스트의 설치본이 컨테이너로 들어가면
# 빌드가 `pnpm install --frozen-lockfile` 이 아니라 남의 PC 상태를 쓰게 되고, 플랫폼이
# 다른 네이티브 모듈이 섞이면 증상이 빌드 실패가 아니라 런타임 오류로 나옵니다.
node_modules
dist
.env
.env.*
!.env.example
