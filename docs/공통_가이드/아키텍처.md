# 아키텍처

> 4절(의존 방향)과 5절(이음매)은 템플릿이 **코드로 이미 강제하고 있는** 부분입니다.
> 1~3절은 이 프로젝트의 목표 구조이며, 세부 범위 확정에 따라 `TBD`가 채워집니다.

## 1. 전체 그림

```mermaid
flowchart TB
    USER(["사용자<br/>제품 정보 텍스트 + 레퍼런스 이미지"])

    subgraph VM["GCP VM 1대 - GPU 1개"]
        FE["frontend<br/>apps/frontend"]
        BE["backend<br/>apps/backend<br/>인증 · 검증 · 잡 수명주기 · 폴백"]
        subgraph GPUBOX["같은 GPU를 두고 경합 - 시간 분할 필요"]
            AE["ai-engine<br/>apps/ai-engine<br/>카피 생성 + 이미지 생성"]
            TR["training/<br/>오프라인 LoRA 학습<br/>상시 기동하지 않음"]
        end
    end

    STORE[("이미지 · 어댑터 저장소<br/>TBD")]
    LLM{{"카피 생성 모델 / API<br/>TBD"}}

    USER -->|"1 . 광고 생성 요청"| FE
    FE -->|"HTTPS"| BE
    BE -->|"2 . jobId 즉시 반환<br/>동기로 붙들지 않음"| FE
    BE -->|"3 . 워커가 잡을 집어 호출<br/>HTTP 내부 계약"| AE
    AE -.->|"외부 호출"| LLM
    TR ==>|"어댑터 가중치 + adapter_card.json<br/>파일 전달, import 아님"| AE
    AE --> STORE
    BE --> STORE

    classDef gpu fill:#fde2e2,stroke:#c0392b,stroke-width:2px
    classDef offline fill:#eef2f7,stroke:#5a6b7d,stroke-dasharray:4 3
    classDef ext fill:#fff6e0,stroke:#c8961e
    class AE gpu
    class TR offline
    class LLM,STORE ext
```

**GCP VM 1대**에 위 컨테이너가 함께 뜹니다. GPU도 1개이므로 **학습은 서비스와 시간을 나눠
씁니다** — 동시에 VRAM을 요구하면 둘 다 OOM으로 죽습니다. 안쪽 상자에 함께 들어간
`ai-engine`과 `training/`이 그 경합 관계이며, 둘을 같은 시간대에 돌리는 것은
"자원이 빠듯한 상태"가 아니라 **양쪽 모두 죽는 상태**입니다.

외부 의존 (스텁 → 실물 추적의 기준):

| 외부 의존 | 용도 | 현재 |
|---|---|---|
| 이미지 생성 base 모델 | 이미지 생성 | TBD (모델 확정 전) |
| 카피 생성 모델/API | 문안 생성 | 스텁 (`StubGenerator`) |
| 오브젝트 스토리지 | 입력·결과 이미지, 어댑터 가중치 | TBD |
| 잡 큐 / 상태 저장소 | 비동기 생성 잡 | TBD |

## 2. 모듈

| 모듈 | 책임 | 위치 | 하지 않는 것 |
|---|---|---|---|
| frontend | 입력 폼, 진행 표시, 결과 갤러리 | `apps/frontend/` | 모델 직접 호출 |
| backend | 인증·입력 검증·잡 수명주기·오케스트레이션·폴백 | `apps/backend/` | GPU 점유, 프롬프트 조립 |
| ai-engine | 카피/이미지 생성, 가드레일, 어댑터 로딩 | `apps/ai-engine/` | 사용자 인증, 잡 상태 관리 |
| training | 데이터 준비, LoRA 학습, 어댑터 카드 생성 | `training/` | 서비스 코드 import |
| contracts | 모듈 간 HTTP 계약 (단일 원천) | `packages/contracts/` | — |
| eval | 재현 가능한 품질 채점 | `apps/ai-engine/eval/` | 모델 호출 (순수 함수) |

**`ai-engine`이 잡 상태를 갖지 않는 것**이 중요합니다. 상태를 가지면 GPU 프로세스 재시작마다
진행 중이던 잡이 사라지고, 스케일아웃 경로도 막힙니다.

## 3. 기술 스택

> `TBD` 항목은 선택의 이유가 자명하지 않으므로 **결정 시 ADR로 남깁니다**.

| 계층 | 기술 | 비고 |
|---|---|---|
| API | FastAPI / Python 3.12 | 확정 |
| 프론트엔드 | TBD | 스캐폴딩 시 `apps/frontend/README.md`의 함정을 먼저 읽을 것 |
| 이미지 생성 | TBD (SDXL / FLUX 등) | VRAM 예산과 **라이선스**가 함께 걸림 → ADR |
| 파인튜닝 | LoRA | 전체 파인튜닝 대비 VRAM·시간 이득. rank는 실측으로 |
| 카피 생성 | TBD (외부 LLM API / 로컬) | 비용·지연·키 관리가 갈림 → ADR |
| 잡 큐 | TBD | 단일 VM이므로 브로커를 새로 세우기 전에 "정말 필요한가"를 먼저 물을 것 |
| 인프라 | docker-compose on GCP VM (GPU) | `infra/` |

## 4. 의존 방향 (위반은 스타일이 아니라 설계 결함)

실선은 **허용된 결합**, 붉은 점선은 **금지된 결합**입니다. 금지선을 넘는 파이썬 import 한 줄이
이 구조가 존재하는 이유를 조용히 없앱니다.

```mermaid
flowchart LR
    subgraph BEAPP["apps/backend"]
        API["src/api<br/>얇은 라우터"]
        CORE["src/backend_core<br/>도메인 · FastAPI 무의존"]
    end
    AE["apps/ai-engine"]
    TR["training/"]

    API -->|"의존"| CORE
    BEAPP -->|"HTTP만<br/>계약: packages/contracts"| AE
    TR ==>|"파일만 - 어댑터 + adapter_card.json<br/>양방향 import 금지"| AE

    CORE -. "import 금지" .-> API
    AE -. "import 금지" .-> BEAPP

    linkStyle 3,4 stroke:#c0392b,stroke-width:2px
```

- **`backend_core`는 `api`를 import하지 않습니다.** 도메인 로직이 FastAPI 없이 실행·테스트
  가능해야 평가 하네스와 오프라인 도구가 그 로직을 직접 부를 수 있습니다.
- **`ai-engine`은 `backend`를 import하지 않습니다(역도 마찬가지).** 두 앱은 별개 배포 단위이고,
  허용된 결합은 HTTP 계약뿐입니다. 이 선을 넘는 파이썬 import 한 줄이 "독립 배포 가능한 AI 모듈"
  이라는 성질을 조용히 없앱니다.
- **`training/`과 `apps/`는 양방향 import 금지.** 학습이 깨져도 서비스는 base 모델로 떠야 하고,
  서비스 리팩터링이 학습 재현성을 건드리면 안 됩니다 ([ADR-0002](../adr/0002-training-outside-apps.md)).
- **라우터는 얇게**: 요청 검증 → 도메인 호출 → 응답 매핑. 라우터 안의 비즈니스 로직은 결함입니다.
- **AI 엔진 내부 파이프라인은 단방향**: 입력 정규화 → 프롬프트 조립 → 생성 → 가드레일 검증 → 출력.
  역방향 의존 금지.

## 5. 이음매 (스텁 → 실물 교체 지점)

워킹 스켈레톤의 외부 의존은 전부 스텁이고, 각 스텁에는 **이름이 붙은 이음매**가 있습니다.
교체는 그 이음매에서 하세요 — 우회해서 옆에 새 경로를 만들면 스켈레톤이 검증하던 성질이 사라집니다.

붉은 노드가 **아직 스텁**, 흰 노드가 **이미 실물**입니다. 요청 하나가 지나가는 순서대로 놓았습니다.

```mermaid
flowchart LR
    REQ(["요청"]) --> CLIENT["api.deps.ai_client<br/>HTTP 클라이언트"]
    CLIENT --> RETR["ai_engine.retrieval<br/>Retriever"]
    RETR --> GEN["ai_engine.generation<br/>Generator"]
    GEN --> IMG["이미지 생성기<br/>미구현"]
    IMG --> GUARD["ai_engine.guardrail<br/>출력 검증"]
    GUARD --> OK(["응답"])
    GUARD -. "근거 부족 / 검증 실패" .-> FB["backend_core.pipeline<br/>FALLBACK_TEXT"]
    FB --> OK
    ADAPT[("어댑터 저장소<br/>미구현")] --> IMG

    classDef stub fill:#fde2e2,stroke:#c0392b,stroke-width:2px
    classDef real fill:#ffffff,stroke:#5a6b7d
    class RETR,GEN,IMG,FB,ADAPT stub
    class CLIENT,GUARD real
```

| 이음매 | 현재 | 교체 후 | 소관 |
|---|---|---|---|
| `ai_engine.retrieval.Retriever` | 번들 픽스처 문자열 매칭 | 브랜드 가이드/제품 정보 조회 | 03 AI(생성) |
| `ai_engine.generation.Generator` | `StubGenerator` (근거 되돌려주기) | 카피 생성 모델 클라이언트 | 03 AI(생성) |
| (신규) 이미지 생성기 | 없음 | base 모델 + LoRA 어댑터 로더 | 03 AI(생성) |
| `api.deps.ai_client` | HTTP 클라이언트 (실물) | 잡 워커 경유로 변경 | 05 백엔드 |
| `backend_core.pipeline.FALLBACK_TEXT` | 자리표시자 문안 | 승인된 폴백 문안·이미지 | 01 PM |
| (신규) 어댑터 저장소 | 없음 | 오브젝트 스토리지 또는 VM 경로 | 05 인프라 |

**이 표가 비워지는 것이 P1의 완료 조건**입니다. 소관 번호는 [역할_가이드/](../역할_가이드/README.md)와 같습니다.

## 6. 결정 기록

구조에 관한 결정은 [docs/adr/](../adr/)에 ADR로 남깁니다. 문서화되지 않은 결정은 결정한 사람이
자리를 비우는 순간 사라집니다. 지금 시점에 **ADR이 필요한 미결정 사항**은 3절의 `TBD` 전부입니다.
