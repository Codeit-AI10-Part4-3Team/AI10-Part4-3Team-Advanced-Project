# 착수 체크리스트

리포 준비(2026-08-01) 시점에 **이미 끝난 것**과 **아직 남은 것**을 구분하는 문서입니다.
착수일(2026-08-04)에 이 문서를 위에서부터 훑으면 무엇을 해야 하는지 알 수 있어야 합니다.

완료한 항목은 `[x]`로 바꾸고 **누가·언제** 했는지 옆에 적으세요. 남아 있는 `[ ]`가
"아직 준비되지 않았다"는 신호입니다. 전부 비면 이 문서는 삭제해도 됩니다.

---

## 0. 이미 끝난 것 (확인만 하세요)

- [x] 템플릿에서 리포 생성 — `Codeit-AI10-Part4-3Team/AI10-Part4-3Team-Advanced-Project` (public)
- [x] `scripts/init_template.py` 실행 (배포판 이름 `adgen`, 환경변수 접두어 `ADGEN_`)
- [x] `training/` 폴더 신설 + `.gitignore`·ruff·pre-commit·이슈 템플릿 배선
- [x] `AGENTS.md` 프로젝트 개요·설계 제약·품질 지표 골격
- [x] `docs/공통_가이드/` 아키텍처·구현 범위·리스크 초안
- [x] `docs/역할_가이드/` `docs/역할_일정/` 7역할 문서 (번호 1:1)
- [x] `docs/adr/0002-학습_파이프라인_배치.md`
- [x] `LICENSE` 저작권자
- [x] 브랜치 보호·머지 전략·라벨 (`scripts/setup-github.sh`)
- [x] AI 리뷰 워크플로 (`.github/workflows/ai-review.yml`) — Greptile 대체.
      시크릿 등록과 앱 설치도 완료됐습니다 (§4)

---

## 1. 사람이 있어야 되는 것 (착수 회의, 2026-08-04)

담당: 01 PM

- [x] **역할 배정** - 5명이 7역할 겸임 (2026-08-10, 01 PM). 금지 조합 확인:
      [역할_가이드/README.md](../역할_가이드/README.md)의 "5명이 7역할을 맡는 방법"
- [x] **팀원을 조직에 초대**하고 리포에 **write 권한** 부여 (2026-08-10)
      (write가 없는 계정은 CODEOWNERS에서 조용히 무시됩니다)
- [x] **`CODEOWNERS`의 `@role-*` 를 실제 GitHub ID로 교체** (2026-08-10) - 전 경로 오너 2명 이상.
      겸임으로 실질 단독 소유가 되는 경로가 없는지도 확인했습니다
- [x] 같은 PR에서 함께 채우기 (2026-08-10): `AGENTS.md` 팀 표,
      [역할_가이드/README.md](../역할_가이드/README.md),
      [개발자_가이드.md](개발자_가이드.md) 5절, 각 역할 파일의 제목
- [x] 교체 머지 후 **`bash scripts/setup-github.sh Codeit-AI10-Part4-3Team/AI10-Part4-3Team-Advanced-Project` 재실행**
      (자리표시자 상태에서는 코드오너 리뷰 요구가 자동으로 꺼져 있습니다).
      확인 2026-08-12: `gh api repos/:owner/:repo/branches/main/protection` 에서
      `require_code_owner_reviews: true`, 승인 1건, required 체크 5종이 모두 등록된 상태입니다
- [x] **`docs/공통_가이드/구현_범위.md` 확정** (2026-08-11 회의) - 스켈레톤 S0 ~ S8 전 행에 DoD와
      소관이 채워졌고, 더미 구현 3곳의 담당까지 정해졌습니다
- [ ] 각자 [역할_일정/](../역할_일정/README.md)의 자기 파일에 작업·DoD 기입
- [ ] 8/17 대체공휴일 확정 여부 재확인 (밀리면 W3 전체가 이동)
- [ ] **PM 소관 차단 항목 닫기** — 개별 항목을 여기 옮겨 적지 않습니다. 무엇이 열려 있고 누가
      판정하는지의 정본은 [미결정_대장.md](../기술문서/미결정_대장.md) 한 곳입니다.
      확정은 회의록으로만 합니다 (기획서 14.3) — 코멘트로는 잠정안만 갱신됩니다

## 2. 기술 결정 (기한 2026-08-08 — 계약 동결일)

담당: 02 테크리드. 각 항목은 **결정 즉시 ADR로**
([adr/README.md](../adr/README.md)의 "아직 쓰이지 않은 ADR" 표).

> 주의: **기한이 지났습니다.** 남은 `[ ]`는 전부 지연 상태이며, 그중 `openapi.yaml` 교체는
> 리스크 12가 실현된 것입니다 ([리스크.md](리스크.md) "실현된 리스크").
> 무엇이 열려 있는지의 정본은 [미결정_대장.md](../기술문서/미결정_대장.md)입니다.

- [ ] **base 이미지 생성 모델 선정** - VRAM 예산 + **상업적 이용/파인튜닝 라이선스 원문 확인**
      (주의: 나중에 뒤집으면 일정 직격입니다. 리스크 3)
      2026-08-11 회의에서 **당분간 외부 API만 쓰기로** 했고, 로컬 모델 채택 여부는 검증
      4순위 결과를 보고 정합니다. **그 방향은 [ADR-0003](../adr/0003-이미지_생성_경로.md)으로
      기록했고, 이 항목은 4순위 결과가 나올 때까지 열려 있습니다.** 라이선스 원문 확인은
      로컬 모델을 채택할 때 필요합니다
- [ ] **카피 생성 경로** (외부 LLM API vs 로컬 모델) - 위 항목과 같은 결정에 묶입니다
- [ ] **비동기 잡 처리 방식** — VM 1대에 브로커를 세울 만한가
- [x] **이미지와 어댑터 저장 위치** (2026-08-09) - **VM 로컬 디스크**, 상태는 SQLite 파일
      ([ADR-0010](../adr/0010-상태_저장소와_파일_보관_위치.md))
- [x] **`packages/contracts/openapi.yaml`을 광고 생성 계약으로 교체**하고 동결
      (미정 필드는 optional로 열어 두고 **먼저 동결**하는 편이 낫습니다)
- [x] **예약된 ADR 두 건 작성** (2026-08-13) — [ADR-0003](../adr/0003-이미지_생성_경로.md)
      이미지 생성 경로, [ADR-0004](../adr/0004-파인튜닝_보류.md) 파인튜닝 보류. 0004는
      "하지 않기로 한 결정"이라 되살릴 조건을 함께 적었습니다

## 3. 환경 (전원, 착수일)

담당: 각자. 상세: [환경_세팅_가이드.md](환경_세팅_가이드.md)

- [ ] `bash scripts/setup-dev.sh` 통과 (pre-push 훅·nbstripout 필터 등록까지 확인)
- [ ] `bash scripts/run-tests.sh` 통과 — **전원이 통과해야 착수 마일스톤 충족**
- [ ] `docker compose -f infra/docker-compose.yml up --build` 로 스택 기동 확인

## 4. GitHub 설정 (API로 안 되는 것 — admin이 콘솔에서)

담당: 02 테크리드 또는 org admin. 배경: [저장소_운영.md](저장소_운영.md) §3

**이 절은 크레딧 삭제 항목 하나만 남았습니다** (2026-08-12, 02 TL 확인). 아래 `[x]`의 상태는
API로 다시 확인한 것이며, 확인 방법을 함께 적어 두었습니다 - 콘솔 설정은 diff에 보이지 않아
"했다고 기억한다"와 "지금 켜져 있다"가 갈릴 수 있기 때문입니다.

- [x] **Settings → General → Features → Discussions 활성화** (02 TL).
      확인: `gh api repos/:owner/:repo --jq .has_discussions` -> `true`
- [x] **Discussions → 카테고리 `CollaborationLog` 생성** (02 TL).
      확인: GraphQL `discussionCategories` 에 `CollaborationLog` 존재
      ⚠️ Discussion 카테고리는 API가 없어 수동 생성만 가능합니다. 없으면 이슈 config의
      협업 일지 링크와 `.github/DISCUSSION_TEMPLATE/`이 동작하지 않습니다
- [x] **Settings → Code security → Secret scanning + Push protection 활성화** (02 TL).
      확인: `gh api repos/:owner/:repo --jq .security_and_analysis` 에서 `secret_scanning` ·
      `secret_scanning_push_protection` 둘 다 `enabled`.
      주의: `secret_scanning_non_provider_patterns` 는 `disabled` 입니다 - 범용 패턴 탐지는
      Advanced Security 구독이 필요하며, 그 사각지대를 pre-commit gitleaks 와 CI 전체 트리
      스캔이 메웁니다 (한 쌍이므로 어느 한쪽도 지우지 마세요)
- [x] (선택) SonarQube Cloud 연결 (02 TL) - 프로젝트 임포트, **Automatic Analysis 끄기**,
      `SONAR_TOKEN` 시크릿 등록(2026-08-01), `sonar-project.properties` 의
      `organization`/`projectKey` 를 실제 값으로 교체(#1).
      확인: `sonarqube.yml` 실행이 PR 에서 `success` - Automatic Analysis 가 켜져 있으면 CI 분석이
      거부되므로, 워크플로가 통과한다는 것이 꺼져 있다는 간접 증거입니다 (높은 신뢰)
- [x] (선택) **AI 리뷰 활성화** (02 TL) - 두 단계 모두 완료.
      1. [github.com/apps/claude](https://github.com/apps/claude) 에서 **Claude GitHub App 설치**
         (⚠️ 빠뜨리면 `Claude Code is not installed on this repository` 로 실패하고
         모든 PR에 빨간 X가 남습니다)
      2. `ANTHROPIC_API_KEY` 시크릿 등록(2026-08-01) + 콘솔 지출 상한 설정
      확인: `ai-review.yml` 최근 실행이 전부 `success` - 앱 미설치 상태에서는 이 잡이 실패하므로
      성공 자체가 1번의 증거입니다. 지출 상한은 Anthropic 콘솔에만 있어 저장소에서 확인할 수
      없습니다 (담당자 확인에 의존)
      ⚠️ 개인 구독 OAuth 토큰(`CLAUDE_CODE_OAUTH_TOKEN`)을 쓰지 마세요. 시크릿은 write 권한자
      전원이 꺼낼 수 있어 개인 계정 자격증명을 5명이 공유하는 구조가 됩니다.
      배경과 비용: [저장소_운영.md §8](저장소_운영.md)
- [ ] 프로젝트 종료 후 `ANTHROPIC_API_KEY` 삭제 (§9 정리 단계와 함께)
      ⚠️ `SONAR_TOKEN` 도 같이 정리하세요. 시크릿은 리포가 남는 동안 계속 유효합니다

## 5. GCP (배포 준비 — W1~W2)

담당: 05 백엔드/인프라.
스펙과 권한 경계는 [ADR-0011](../adr/0011-배포_환경_스펙과_권한_경계.md)에서 확정됐습니다 —
**스펙 상향이나 권한 확대를 전제로 계획하지 마세요.**

### 5-a. 우리가 하는 것

- [x] 상속되는 조직 정책 확인 (2026-08-10) — 외부 IP `ALLOW` · 이미지 프로젝트 제한 없음 ·
      Shielded VM 미강제 · 시리얼 포트 접근 가능 · **SA 키 발급 금지**.
      상세: [infra/README.md](../../infra/README.md)
- [x] **권한 범위 실측** (2026-08-10) — 커스텀 역할의 정의는 조회할 수 없어(`iam.roles.get` 거부)
      만들었다 지우는 프로브로 확인했습니다. **고정 IP 예약, 방화벽 규칙 생성,
      스냅샷 스케줄 생성, API 활성화 전부 가능**. 컴퓨트 계열 제약 없음 (리스크 11 해소)
- [x] **고정 외부 IP 예약** (2026-08-10) - 리소스 `ai10-part4-team3-ip`.
      값은 저장소에 적지 않습니다 ([infra/README.md](../../infra/README.md))
- [x] **SSH 접근 경로 확정** (2026-08-10) — IAP 대역으로 제한을 **시도했다가 되돌렸습니다.**
      `iap.tunnelInstances.accessViaIAP` 권한이 없어(`4033`) 터널이 불가하고, 그 권한은 우리가
      부여할 수 없습니다. **22번을 `0.0.0.0/0`으로 두는 것이 의도된 상태입니다 — 다시 잠그지
      마세요.** 경위와 복구 경로: [infra/README.md](../../infra/README.md) "SSH 접근 경로"
- [x] **스냅샷 스케줄 + 보존 7일** (2026-08-10) — 정책 `adcraft-daily-snap`, UTC 18:00.
      ⚠️ 콘솔에서 이미 만든 스케줄이 있으면 **정책이 두 개 붙습니다.** 하나만 남는지 확인
- [x] **OS 이미지 선정** (2026-08-10) — `Deep Learning VM with CUDA M132`
      (Ubuntu 24.04, CUDA 12.9). PyTorch 미포함 판을 골라 디스크와 인터프리터 혼선을 함께 줄임
- [x] **VM 프로비저닝 + 컨테이너 안에서의 GPU 인식 확인** (2026-08-10) — 드라이버는 이미지가
      해결. **Docker는 이 이미지에 없어 수동 설치**했습니다. 절차와 실측값:
      [infra/README.md](../../infra/README.md) "프로비저닝 재현 절차"
- [x] **swap 16GB 확보** (2026-08-10) — 호스트 RAM 16GB 고정이고 상향 불가입니다 (ADR-0011).
      `/etc/fstab` 등록 + `vm.swappiness=10`, 재부팅 후 유지 확인.
      ⚠️ swap은 **메모리 증설이 아니라 안전망**입니다. 상시로 초과하면 thrashing으로 학습이
      사실상 멈추므로, 그때는 worker 수를 줄여야 합니다. **VRAM에는 swap이 없습니다** (리스크 1)
- [~] **ai-engine을 루프백에 바인딩** (`127.0.0.1:8100:8100`): 방화벽 권한 없이도 외부 노출을
      원천 차단합니다. 방화벽은 이중 방어선이지 유일한 방어선이 아니어야 합니다.
      **2026-08-12 (05 임동규) 두 가지까지 확인했습니다**: `infra/docker-compose.yml`이 이미
      루프백에 바인딩하고 있고, **8100을 여는 방화벽 규칙이 0건**입니다
      ([infra/README.md](../../infra/README.md) 권한 경계 표).
      **외부 실측(밖에서 8100에 닿지 않음)은 보류합니다.** 스택이 VM에 올라가야 측정이
      성립하는데, 지금은 8000과 8100이 모두 닫혀 있어 "막혔다"와 "안 떠 있다"가 구분되지
      않습니다. 배포 시점에 8000이 열리고 8100이 닫힌 것을 함께 확인해야 이 항목이 닫힙니다
- [~] docker 데몬 `systemctl enable` + **재부팅 1회 실검증** (리스크 10) -
      2026-08-10 **데몬 수준까지만** 검증(재부팅 후 `enabled` + `active`).
      **compose 스택 자동 복구는 배포 후 재부팅으로 한 번 더 확인해야 이 항목이 닫힙니다**
- [~] 디스크 사용률 감시 — Ops Agent `2.70.0` 설치 완료(2026-08-10). **알림 정책은 미설정**이라
      아직 아무도 통보받지 못합니다. Cloud Monitoring에서 디스크 85% 알림을 걸어야 닫힙니다.
      100GB가 상한이고 확장에도 권한이 필요합니다
- [ ] SQLite 정합 백업 cron(`VACUUM INTO`) + **백업 파일을 주기적으로 로컬에 내려받기** (리스크 9)
- [ ] **학습 1회전 소요 시간 실측** — 짧은 스텝으로라도. 중간 점검(08-17)의 "파인튜닝 1회전"
      달성 가능 여부가 이 숫자에 달려 있습니다
- [ ] **W2 중 빈 스택이라도 1회 배포**: 마지막 주에 처음 올리면 그것이 곧 사고입니다.
      주의: **날짜가 [역할_일정/05-백엔드_인프라.md](../역할_일정/05-백엔드_인프라.md)와
      어긋납니다** (2026-08-12, 05 임동규 발견). 이 줄과 [infra/README.md](../../infra/README.md)는
      "W2 중"(08-12 ~ 08-14)이고, 역할 일정과 [00-overall.md](../역할_일정/00-overall.md) gantt는
      **08-20**입니다. 목적 문장("마지막 주 전에 환경 차이를 드러낸다")은 양쪽이 같아 **같은
      작업이 두 날짜로 적힌 것**으로 보입니다. 어느 쪽이 맞는지는 여기서 정하지 않습니다.
      일정 확정 근거는 회의록이므로(기획서 14.3) 다음 회의 안건입니다.
      갱신(2026-08-13): [회의록](../회의록/2026-08-13_워킹스켈레톤_일정_조정.md) 정정 절로
      **완료 여부가 정해졌습니다.** 08-12에 끝난 것은 VM 환경 설정이고 이 항목(스택 배포)은
      미완료입니다. 세 항목의 구분은 [00-overall.md](../역할_일정/00-overall.md) 5-a절에
      있습니다. **날짜는 08-14로 맞췄습니다**(TL 초안, 확정 근거는 회의록) - 이 줄의
      "W2 중"과 역할 일정의 08-20이 어긋나 있던 것이 해소되었습니다.
      주의: **리허설로 올릴 때 `ADGEN_MODEL_API_KEY`를 비워 두세요.** 8000은 태그 없는 규칙으로
      이미 외부에 열려 있고 S0 인증은 아직 없습니다. 키를 채우면 인증 없는 공개 엔드포인트가
      유료 API를 부르는 구성이 됩니다
- [ ] `infra/.env.example`을 실제 키 **이름**으로 갱신 (⚠️ 값은 절대 커밋하지 말 것)
- [ ] Actions Secrets/Variables 등록 (워크플로가 참조하는 것이 있다면)
- [ ] 학습 vs 추론 **GPU 시간 분할 규칙** 합의 (리스크 1)

### 5-b. 학원에 요청하는 것 (우리 권한 밖)

**2026-08-12 기준 요청할 항목이 없습니다.** 컴퓨트 계열 권한이 전부 열려 있고 크레딧 문의도
답을 받았습니다. 나중에 막히는 것이 생기면 **요청 내용과 결과(승인/거부)를
`infra/README.md`에 기록**하세요 — 우회 구성을 만들면 재현 절차가 학원 환경과 갈라집니다.

- [x] **잔여 크레딧과 소진 예상 시점** 확인 (2026-08-12, 02 TL) - 학원 답변은 **"감당 가능하니
      신경 쓰지 않아도 된다"**입니다. 주기적 확인 항목에서 내립니다.
      ⚠️ 근거 등급은 **구두 확인**입니다 ([AGENTS.md](../../AGENTS.md) 근거 자료 규칙) - 크레딧을
      이유로 스펙이나 일정을 바꾸는 판단을 하려면 회의록이나 이슈로 남겨야 합니다.
      ⚠️ [리스크.md](리스크.md) 9번의 **대응은 그대로 유지합니다.** 시연 산출물을 미리 로컬로
      내려받는 것은 크레딧뿐 아니라 VM 장애·보존 기간 만료에도 걸리는 대비이기 때문입니다

### 5-c. 하지 않는 것

배정 스펙과 결제 구조 때문에 사라진 항목입니다. 되살리려면 ADR-0011부터 고치세요.

- ~~GCP 프로젝트 생성 + 결제 계정 연결~~ — 학원이 배정합니다
- ~~예산 알림(임계값) 설정~~ — 결제 계정 권한이 없습니다
- ~~GPU 할당량(quota) 신청~~ — 배정 스펙에 이미 포함되어 있습니다
- ~~비용 절감용 VM 정지 스케줄~~ — 절감 대상이 우리 비용이 아닙니다. 정지는 GPU 시간 분할
  규칙(리스크 1)이 요구할 때만 합니다
- ~~GitHub Actions에서 VM으로 자동 배포~~ — `iam.disableServiceAccountKeyCreation`이 조직 수준에서
  적용되어 **SA 키를 발급할 수 없습니다.** 배포는 SSH + `git pull` + `docker compose up` 수동
  경로이며, 절차를 `infra/README.md`에 남깁니다 (ADR-0011)

> ⚠️ **스냅샷 보존 기간이 데이터 보존 정책과 충돌할 수 있습니다.**
> [세션_보관_정책.md](../기술문서/세션_보관_정책.md) 2절이 결과 이미지·브리프·시안의 보존 기간을
> **7일**로 정하고 일 1회 정리 배치가 실제로 지웁니다. 그런데 스냅샷을 30일 보관하면 지웠어야 할
> **업로드 사진이 스냅샷 안에 남습니다.** 개인정보이거나 저작물일 수 있는 데이터입니다.
> **스냅샷 보존을 7일에 맞추는 쪽**을 권합니다 (일 1회 x 7일 = 7개).

## 6. 데이터 (학습 시작 전 필수 — W1~W2)

담당: 04 AI 학습·데이터

- [ ] 브랜드 데이터셋 확보 + `training/data/<dataset-id>/` 배치
- [ ] **`training/data/README.md`의 출처 대장 작성** — 권리 근거·확인자·확인일
      주의: **대장이 비어 있으면 학습을 시작하지 않습니다.** 학습된 가중치는 되돌릴 수 없습니다 (리스크 4)
- [ ] 캡션에서 개인정보 제거 (`prepare` 단계에서 — 학습 후에는 제거 불가)
- [ ] `training/configs/brand-lora.example.yaml`을 실제 config로 복사·기입

## 7. 코드 (P1 진입 — W1 이후)

- [ ] `apps/ai-engine/src/ai_engine/fixtures/corpus.jsonl` 을 실제 데이터로 교체
      ⚠️ 교체 전까지 **품질 숫자를 보고하지 마세요** (자기 자신과의 일치율입니다)
- [ ] `apps/ai-engine/eval/golden_dataset/` 골든셋 실물화 — 담당: 07 QA
- [~] `backend_core.pipeline.FALLBACK_TEXT` **제거** - 담당: 03 AI(생성)
      승인 문안 폴백은 [ADR-0005](../adr/0005-열화_폴백은_자동화_생략으로_한정.md)로 **폐기**되었습니다.
      카피는 제품마다 달라 사전 승인 응답이 성립하지 않습니다. 채울 것이 아니라 지울 자리이며,
      이음매 교체와 같은 PR에서 없앱니다
- [ ] **보존 기간 정리 배치 구현** — 담당: 05 백엔드.
      [세션_보관_정책.md](../기술문서/세션_보관_정책.md) 2절이 업로드 사진 24시간, 브리프·시안·결과
      이미지 7일, 로그 30일로 정했고 **일 1회 배치가 실제로 지워야** 합니다.
      ⚠️ 기간을 문서에만 적어 두면 아무것도 지워지지 않습니다. 그 상태로 배포하면 개인정보이거나
      저작물일 수 있는 데이터를 기간 없이 쌓는 것이고, [AGENTS.md](../../AGENTS.md)의 하드 제약을
      정책이 스스로 어기게 됩니다. 기간 값과 배치 주기는 설정값으로 둡니다
- [ ] `apps/frontend/` 스캐폴딩 (⚠️ 먼저 `apps/frontend/README.md`의 함정과
      루트 `.gitignore`의 `lib/` 패턴을 읽을 것)
- [ ] `.github/dependabot.yml` 의 npm 블록 주석 해제 (프론트 스캐폴딩 후)
- [ ] **`frontend-ci.yml` 의 스캐폴딩 가드 제거** (프론트 스캐폴딩 후) —
      `if: hashFiles(...)` 조건과 `--if-present` 를 지우세요.
      ⚠️ 그대로 두면 스크립트가 사라지거나 이름이 바뀐 뒤에도 잡이 **조용히 초록**으로 통과합니다.
      "아직 없어서 건너뜀"과 "없어져서 검사 안 됨"이 CI에서 구분되지 않습니다
- [ ] [아키텍처.md](아키텍처.md) 5절 **이음매 표를 비우기** = P1 완료 조건

## 8. 제출물 준비 (판본을 고정하는 시점 — 중간 점검 08-17, 제출 08-31)

담당: 기획서 판본은 01 PM(정본 소관), 점검 실행은 각 문서의 소관자.
규약: [문서_작성_규약.md](문서_작성_규약.md)

제출 직전에 몰리면 반드시 빠뜨리므로 항목으로 남깁니다. **PDF로 변환한 뒤에는 이미 치환되거나
사라진 문자를 찾을 수 없습니다** — 점검은 원본 마크다운에 돌립니다.

- [ ] **비ASCII 문장부호 점검**을 제출 대상 문서 전체에 실행
      ([문서_작성_규약.md](문서_작성_규약.md) 5절의 스크립트).
      대상은 기획서·기술문서·ADR·리뷰 코멘트이며, `AGENTS.md`·`README`·가이드류는 아닙니다
- [ ] **mermaid 노드 라벨을 따로 확인** — 5절 스크립트는 펜스 코드 블록을 건너뛰는데, 노드 라벨은
      보고서에 **보이는 텍스트로 렌더링**됩니다. 검사에서 빠지지만 결과물에는 나옵니다
- [ ] **기획서 판본 태그 + Release 자산 첨부** ([기획서/README.md](../기획서/README.md) 개정 절차).
      PDF는 커밋하지 않고 Release에만 붙입니다. 버전은 파일명이 아니라 태그가 구분합니다
- [ ] 시연·발표 산출물을 **보존 기간과 무관하게 미리 로컬로 내려받기**
      ([세션_보관_정책.md](../기술문서/세션_보관_정책.md) 2절, 리스크 9).
      만료 후 복구 경로는 없습니다

## 9. 정리 (전부 끝난 뒤)

- [ ] `docs/TEMPLATE_GUIDE.md` · `docs/DESIGN_DECISIONS.md` 삭제 (템플릿 전용 문서)
- [ ] `docs/역할_가이드/00-역할_템플릿.md` 는 **남겨 두세요** (역할 추가 시 사용)
- [ ] 이 문서 삭제

---

## 앱/폴더를 추가할 때 (상시 규칙)

체크리스트가 아니라 **규칙**입니다. 어기면 모든 PR이 조용히 막힙니다.

| 고칠 곳 | 빠뜨리면 |
|---|---|
| 루트 `pyproject.toml`의 ruff `src` | isort가 앱 내부 import를 서드파티로 보고 CI가 I001로 실패 |
| `.github/workflows/ci.yml`의 matrix | 새 앱이 검사되지 않음 |
| `scripts/setup-github.sh`의 `contexts` | required check 이름 불일치 → 머지가 영원히 `Expected` 대기 |
| `CODEOWNERS` | 리뷰어 자동 배정 누락 |

**네 곳을 같은 PR에서** 고치세요. required check의 context 이름은 잡의 `name:`과
문자열이 정확히 일치해야 하고, required 워크플로에는 `paths:` 필터를 절대 넣지 않습니다.
