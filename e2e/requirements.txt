# e2e 하네스 의존성 —  pip install -r e2e/requirements.txt
# apps/*의 [dev] extra와 분리돼 있습니다: E2E는 별도 워크플로(non-required)에서만 돌므로
# required CI가 Playwright/브라우저를 끌고 오지 않게 하기 위함입니다.
pytest>=8.0.0
httpx>=0.27.0          # 기동된 서비스에 HTTP로만 접근 (앱 파이썬 import 금지 규약)
playwright>=1.45.0     # 브라우저 시나리오용 (프론트엔드가 없으면 지워도 됩니다)
pytest-playwright>=0.5.0
