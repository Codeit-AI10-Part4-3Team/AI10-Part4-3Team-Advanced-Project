# 04. AI 엔지니어 (학습·데이터) — 일정

> 역할 정의는 [역할_가이드/04-AI_학습_데이터.md](../역할_가이드/04-AI_학습_데이터.md)에 있습니다.
> **전체 일정과 어긋나면 [00-overall.md](00-overall.md)가 맞습니다.**

## 작성 규칙

- 날짜는 **절대 표기**(`2026-08-10`). "다음 주"는 나중에 읽을 수 없습니다.
- 각 작업에 **완료 조건(DoD)**을 적으세요. "구현한다"는 완료 조건이 아닙니다.
- 일요일과 공휴일(8/15, 8/17)은 작업일이 아닙니다.

## 이 트랙은 조건부입니다

> TL 초안입니다. 확정은 회의록으로 합니다 ([00-overall.md](00-overall.md) 머리말).

**2026-08-11 회의에서 당분간 외부 API만 쓰기로 했습니다.** 로컬 모델 채택 여부는 검증 4순위
(로컬 모델과 GPT 비교, IP-Adapter 효과) 결과를 보고 정합니다
([미결정_대장.md](../기술문서/미결정_대장.md) A-5).

따라서 **파인튜닝은 크리티컬 패스에 없습니다.** 아래 표는 두 갈래로 나뉘며, 어느 쪽이 살아나는지는
08-21에 정해집니다. 03과 같은 사람이 겸임하므로, 채택하지 않으면 그 시간은 03의 이음매 교체와
가드레일 작업으로 갑니다.

## 갈림길까지 (채택 여부와 무관하게 하는 일)

| 작업 | 시작 | 완료 목표 | 완료 조건(DoD) | 상태 |
|---|---|---|---|---|
| 검증 8순위 VM 스펙 확인 | 08-12 | 08-12 | 가용 VRAM 22.5GB에서 후보 모델이 뜨는지 확인 (ADR-0011) | 대기 |
| 검증 4순위 준비 | 08-18 | 08-19 | 비교 조건(프롬프트, 해상도, 시도 횟수)을 미리 고정 | 대기 |
| **검증 4순위 실행과 판정** | 08-20 | 08-21 | 채택 여부를 판단할 수 있는 비교 결과. 판정자는 AI 담당 제외 전원 | 대기 |

## 채택하는 경우에만 (08-21 이후)

| 작업 | 시작 | 완료 목표 | 완료 조건(DoD) | 상태 |
|---|---|---|---|---|
| 데이터 권리 대장 작성 | 08-21 | 08-22 | `training/data/README.md`에 출처, 권리 근거, 확인자, 확인일. **비어 있으면 학습을 시작하지 않습니다** (리스크 4) | 조건부 |
| 캡션에서 개인정보 제거 | 08-22 | 08-22 | `prepare` 단계에서. 학습 후에는 제거할 수 없습니다 | 조건부 |
| 학습 1회전 | 08-24 | 08-25 | `training/configs/`의 config만으로 재현 가능. `adapter_card.json` 산출 | 조건부 |
| 어댑터 ADR | 08-25 | 08-26 | 어댑터 전달 방식과 서빙 연결을 ADR로 (08-11 회의 결정) | 조건부 |
| 어댑터 적용 결과 비교 | 08-26 | 08-27 | 같은 프롬프트에서 base 대비 톤 차이가 눈으로 구분됨 | 조건부 |

**GPU는 한 대입니다.** 학습을 돌리는 동안 추론이 VRAM을 요구하면 둘 다 죽습니다. 학습을 시작하기
전에 팀에 알리고, 시간을 나눠 쓰세요 (리스크 1). 08-24 이후는 배포와 실측이 겹치는 구간이라
**학습을 그 위에 얹는 것 자체가 위험합니다.**

## 채택하지 않는 경우

`training/`은 이번 범위에서 비어 있는 채로 남습니다. 그 사실과 이유(검증 4순위 결과)를
`training/README.md`에 적어 두세요. **비워 둔 것과 잊어버린 것은 문서로만 구분됩니다.**

## 의존 관계 (내가 막히면 누가 멈추는가 / 내가 기다리는 것)

| 방향 | 상대 역할 | 무엇 | 언제까지 |
|---|---|---|---|
| **막음** | 02 | 검증 4순위 결과. 이미지 생성 경로 ADR의 재검토 조건이 이 결과입니다 | 08-21 |
| 기다림 | 05 | VM 접근과 GPU 시간 분할 합의. **VM 환경 설정은 완료**이므로 접근 자체는 막혀 있지 않고, 남은 것은 시간 분할 합의입니다 ([00-overall.md](00-overall.md) 5-a절) | 08-20 |
| 기다림 | 01 | 채택 시 데이터 출처와 권리 범위 판단 | 08-22 |
