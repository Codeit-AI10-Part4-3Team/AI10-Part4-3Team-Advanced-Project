# 실물 모드 시나리오 기록 시트

2026-08-29 회차의 기록입니다. 왜 이 조합인지는
[계획서](실물모드_시나리오_테스트_계획서.md)에 있습니다.

| 수행 | 실행 일자 | 시작 | 종료 | 생성 모드 |
|---|---|---|---|---|
| 임동규 | 2026-08-29 | 19:30 | 21:30 | `model` |

**프롬프트 판이 유형별로 다릅니다.**

| 유형 | 판 | |
|---|---|---|
| 만화형 8건 | **`draft-v2`** | PR #332 반영 후 다시 돌았습니다. 8건이 같은 판입니다 |
| 단일 광고형 11건 | `draft-v1` | `draft-v2` 의 변경(`PRODUCT_STAGING`)은 `_comic_instructions` 에만 들어가므로 **단일 광고형 프롬프트 문장은 두 판이 동일합니다.** 판 이름만 다릅니다 |

**4절의 판정 열 셋은 만화형만 대상이고, 그 8건은 전부 `draft-v2` 입니다.**

**화면에서 입력하고, 나온 것을 보고, 한 줄 적습니다.** 한 번에 한 세션씩 합니다.

---

## 1. 시작 전

`.env` 에 두 줄이 있어야 합니다.

```
ADGEN_GENERATION_MODE=model
ADGEN_MODEL_API_KEY=<키>
```

**타임아웃은 넣지 않습니다.** 기본값이 25초 / 30초입니다 (PR #329). 아래 확인 줄로 그 값이
실제로 들어갔는지만 봅니다.

```bash
cd /srv/adcraft/app
C="docker compose -f infra/docker-compose.yml"

$C logs ai-engine | tail -20 | grep "generation seams"   # 'model' mode
$C exec ai-engine sh -c 'echo engine=$ADGEN_BRIEF_FILL_MODEL_TIMEOUT_S'   # 25.0
$C exec backend   sh -c 'echo backend=$ADGEN_BRIEF_FILL_TIMEOUT_S'        # 30.0
$C exec ai-engine sh -c 'echo keylen=${#ADGEN_MODEL_API_KEY}'   # 0 이 아님
$C exec backend python -c "import json,os;print(len(json.loads(os.environ['ADGEN_ART_STYLES'])))"   # 8
$C exec backend sh -c 'ls /data/art-styles | wc -l'      # 8

# 키가 모델에 실제로 닿는가. 안 보면 첫 유료 호출인 S01 에서 처음 알게 됩니다
$C exec ai-engine python -c "import os;from openai import OpenAI;print(len(OpenAI(api_key=os.environ['ADGEN_MODEL_API_KEY']).models.list().data))"

# 품질 override 가 비었는가. 값이 있으면 만화형이 medium 이 아닌 티어로 돕니다
$C exec ai-engine sh -c 'echo "override=[${ADGEN_IMAGE_QUALITY_OVERRIDE}]"'

# 08-29 프롬프트가 실제로 배포됐는가. 'draft-v2' 가 아니면 옛 칸 지시로 돕니다
$C exec ai-engine python -c "from ai_engine import draft_prompt; print(draft_prompt.VERSION)"
```

마지막 셋도 요금 0 입니다. **`override=[]` 가 아니면 멈추십시오** - 값이 있으면 요청의 티어를
덮어쓰고 경고 로그만 남기므로, 만화형 8세션이 다른 티어로 돌아 단가 표와 화풍 축이 함께
어긋납니다. 증상은 "결과가 좀 흐리다" 뿐이라 회차가 끝날 때까지 모릅니다.

**`draft-v2` 가 아니어도 멈추십시오** - 배포된 코드가 2026-08-29 조치 이전이라는 뜻이고,
그러면 1번 칸에 제품이 없는 옛 칸 지시로 돕니다 (생성_파이프라인 4.2절). 4절의 판정 열
셋이 통째로 다른 조건의 값이 되며, 이것도 결과만 보고는 알 수 없습니다.

**셋째 줄과 넷째 줄은 다른 것을 봅니다.** 셋째는 **카탈로그 설정값**이 제대로 파싱되는지이고,
넷째는 볼륨의 **예시 이미지 파일** 수입니다. 키를 `.env` 에 넣을 때 개행을 빠뜨리면 키가
`ADGEN_ART_STYLES` 값 끝에 이어 붙는데, **그 사고는 셋째 줄만 잡습니다** - 이미지 파일은
멀쩡하므로 `ls` 는 그대로 8을 돌려줍니다.

화면에서 화풍 격자가 **8칸** 다 보이는지도 눈으로 확인합니다. 하나라도 어긋나면 멈춥니다.

---

## 2. 입력값 (그대로 칩니다. 사진은 `테스트이미지/`)

| | 제품명 | 제품 특징 | 추가 요청 |
|---|---|---|---|
| A | 플렌느 프리미엄 디퓨저 블랙체리 | 블랙체리 향의 실내용 리드 디퓨저입니다. 200ml 유리병에 리드 스틱 다섯 개가 들어 있고, 거실이나 침실에 두고 씁니다. | 혼자 사는 20대와 30대가 주 타겟입니다. 퇴근하고 집에 들어왔을 때의 편안한 분위기로 부탁합니다. |
| B | 한돈 삼겹살 | 국내산 돼지고기 삼겹살입니다. 5mm 두께로 썰어 구우면 기름이 빠르게 빠지고, 500g 한 팩에 3 ~ 4인분이 들어 있습니다. 냉장으로 배송합니다. | 주말에 가족과 고기를 굽는 30대와 40대가 주 타겟입니다. 가족이 둘러앉은 식탁을 배경으로 해 주세요. |
| C | 아디다스 워시드 야구 모자 | 워시드 가공한 면 소재 볼캡입니다. 앞면에 트레포일 로고가 자수로 들어가 있고 뒤쪽 스트랩으로 머리 둘레를 조절합니다. 연하늘색 한 가지입니다. | 캠퍼스를 오가는 20대 대학생이 주 타겟입니다. 주인공으로 나오면 좋겠습니다. |
| D | 코카콜라 | 유리병에 담은 콜라입니다. 350ml 용량이고 차게 두었다가 병째로 마시는 제품입니다. 탄산이 강해 식사와 함께 마시기 좋습니다. | 10대와 20대가 주 타겟입니다. 북극곰 캐릭터가 등장하게 해 주세요. |
| E | 원더로스팅 블렌딩 홀빈 원두 1kg | 블렌딩 원두를 분쇄하지 않은 홀빈 상태로 담았습니다. 1kg 한 봉지이고 중배전이라 산미와 단맛이 함께 납니다. 그라인더로 갈아 씁니다. | 집에서 커피를 내려 마시는 30대가 주 타겟입니다. 아침 햇살이 드는 조용한 카페 분위기로 부탁합니다. |
| F | 델리 메탈 샤프펜슬 | 금속 몸체의 0.5mm 노크식 샤프펜슬입니다. 무게중심이 아래쪽에 있어 오래 써도 손이 덜 피로하고, 흰색 분홍색 하늘색 세 가지가 있습니다. | 시험을 준비하는 10대와 20대 학생이 주 타겟입니다. 시험 기간에 집중하는 상황으로 부탁합니다. |
| G | 비쿨 미니 휴대용 선풍기 | 손에 쥐고 쓰는 휴대용 선풍기입니다. USB로 충전하고 풍량은 3단계이며, 민트 분홍 파랑 세 가지 색이 있습니다. 가방에 넣고 다닐 수 있는 크기입니다. | 야외 활동이 많은 20대와 30대가 주 타겟입니다. 더워서 선풍기를 꺼내 쐬는 장면으로 부탁합니다. |
| H | 트렉 SLR7 AXS 로드 자전거 | 카본 프레임 로드 자전거입니다. 무선 변속 구동계와 디스크 브레이크를 쓰고 높은 림의 에어로 휠이 들어갑니다. 장거리 주행과 업힐을 함께 염두에 둔 구성입니다. | 주말 라이딩을 즐기는 30대와 40대가 주 타겟입니다. 자전거를 타고 달리는 장면으로 부탁합니다. |

**추가 요청의 첫 문장(타겟)을 빼지 마십시오.** 여덟 개 전부에 있습니다. 빼면 `brief:fill`
이 되묻습니다 - 프롬프트가 브랜드 추측을 금지해서, 물리적 묘사만으로는 연령대를 읽을 수
없습니다 (계획서 C-b-1절).

**제품 특징은 시험용으로 작성한 가상 정보입니다.** 실제 제품 사양이 아니므로 결과물을 발표
자료에 실을 때 그 사실을 함께 적어야 합니다.

---

## 3. 회차 기록

**결과**: `좋음` / `보통` / `이상함` / `실패`. 이상하거나 실패면 메모에 무엇이 이상한지 적습니다.
**파일명**: 받는 즉시 회차 번호를 붙입니다. 안 그러면 제품명이 같아 겹칩니다.
**메모**: 카피에 근거 밖 문구(없는 수치, 타사 비교, 최상급, 수상)가 있으면 **`위반 O` 와 그
문구**를 먼저 적습니다. 없으면 `위반 X`. 19건이 모이면 그것이 카테고리 8종으로 넓힌 표본이고
추가 비용은 0 입니다.

| 회차 | 유형 | 화풍 | 제품 | 결과 | 파일명 | 메모 |
|---|---|---|---|---|---|---|
| *예시* | *단일* | *5 감성 수채화* | *E 원두* | *좋음* | *`S14_원두_단일.webp`* | *위반 X. 카피에 1kg 들어감* |
| S01 | 단일 | 3 SD 치비 | C 모자 | 보통 | `S01_아디다스 워시드 야구 모자_단일.webp` | 위반 X. 로고가 하얀색으로 변질됨 |
| S02 | 단일 | 4 레트로 팝 | D 콜라 | 좋음 | `S02_코카콜라_단일.webp` | 위반 X |
| S03 | 단일 | 8 시네마틱 | H 자전거 | 좋음 | `S03_트렉 SLR7 AXS 로드 자전거_단일.webp` | 위반 X |
| S04 | **만화** | 5 감성 수채화 | E 원두 | 이상함 | `S04_원더로스팅 블렌딩 홀빈 원두 1kg_만화.webp` | 위반 X. 이미지 일관성은 유지되나 제품 이미지가 입력 이미지와 다름 |
| S05 | 만화 | 1 심플 플랫 | A 디퓨저 | 이상함 | `S05_플렌느 프리미엄 디퓨저 블랙체리_만화.webp` | 위반 X. 이미지 일관성은 유지되나 제품 이미지가 입력 이미지와 다름 |
| S06 | 만화 | 2 한국형 일상 | B 삼겹살 | 좋음 | `S06_한돈 삼겹살_만화.webp` | 위반 X. 1컷에 불판에 있던 고기가 2컷에서 사라지는 약간의 문제가 있음, 5번 컷 오탈자. **4절 셋째 열 X** (여덟 세션 중 유일)|
| S07 | 만화 | 3 SD 치비 | C 모자 | 보통 | `S07_아디다스 워시드 야구 모자_만화.webp` | 위반 X. 5, 6컷에서 캐릭터가 모자를 썼으나 벤치에 모자가 그대로 남아있음 |
| S08 | 만화 | 4 레트로 팝 | D 콜라 | 좋음 | `S08_코카콜라_만화.webp` | 위반 X |
| S09 | 만화 | 6 에디토리얼 | F 샤프 | 이상함 | `S09_델리 메탈 샤프펜슬_만화.webp` | 위반 X. 제품 이미지는 잘 나왔으나 2,4,6컷 사람 위치가 이상하며 사람의 복장 일관성이 없음 |
| S10 | 만화 | 7 3D 클레이 | G 선풍기 | 보통 | `S10_비쿨 미니 휴대용 선풍기_만화.webp` | 위반 X. 5컷에서 제품 특징에 있는 usb 충전 내용을 설명하려다 어색한 이미지가 들어감. + 선풍기를 포장 그대로 사용하고 있음 (사용은 1 ~ 3번 칸이 아님) |
| S11 | 만화 | 8 시네마틱 | H 자전거 | 좋음 | `S11_트렉 SLR7 AXS 로드 자전거_만화.webp` | 위반 X. 제품 이미지를 잘 반영하였고 일관성 있음 |
| S12 | 단일 | 1 심플 플랫 | A 디퓨저 | 이상함 | `S12_플렌느 프리미엄 디퓨저 블랙체리_단일.webp` | 위반 X. 제품 이미지가 입력과 다름 |
| S13 | 단일 | 2 한국형 일상 | B 삼겹살 | 보통 | `S13_한돈 삼겹살_단일.webp` | 위반 X. |
| S14 | 단일 | 5 감성 수채화 | E 원두 | 이상함 | `S14_원더로스팅 블렌딩 홀빈 원두 1kg_단일.webp` | 위반 X. 제품 이미지가 입력과 다름 |
| S15 | 단일 | 6 에디토리얼 | F 샤프 | 보통 | `S15_델리 메탈 샤프펜슬_단일.webp` | 위반 X. 제품 이미지와 입력이미지가 다소 다름 |
| S16 | 단일 | 7 3D 클레이 | G 선풍기 | 보통 | `S16_비쿨 미니 휴대용 선풍기_단일.webp` | 위반 X. 제품 이미지와 입력이미지가 다소 다름 |
| S17 | 단일 | **1 심플 플랫** | E 원두 | 이상함 | `S17_원더로스팅 블렌딩 홀빈 원두 1kg_단일.webp` | 위반 X. 제품 이미지가 입력과 다름 + 블렌딩이 블롄딩으로 읽힘|
| S18 | 단일 | **6 에디토리얼** | E 원두 | 이상함 | `S18_원더로스팅 블렌딩 홀빈 원두 1kg_단일.webp` | 위반 X. 제품 이미지가 입력과 다름|
| S19 | 단일 | 5 감성 수채화 | **E 원두, 추가 요청에 타겟 문장만** | 이상함 | `S19_원더로스팅 블렌딩 홀빈 원두 1kg_단일.webp` | 위반 X. 제품 이미지가 입력과 다름 |

**S01 ~ S03 은 브랜드 셋을 먼저 떠보는 것입니다.** 거절이 나오면 그 화풍의 만화형
(S07, S08, S11)을 취소합니다.

**S04 에서 실패하면 멈추고 보고합니다.** 만화형이 제일 비쌉니다 (한 회차 약 0.43 USD,
약 3분). 만화형 실패가 2건 쌓이면 만화형은 접고 단일 광고형만 진행합니다.

**`이상함` 8건의 사유는 전부 "제품 이미지가 입력 사진과 다름" 입니다.** 그것은 계획서
A-c 가 **물을 수 없다고 미리 잘라 둔 축**입니다 - 사진은 렌더 호출(`ImageRenderRequest`)에
들어가지 않으므로 제품 모양은 제품명과 소구점 텍스트만 보고 그려집니다. **제품 경로의
결함률로 읽지 마십시오.**

**S17, S18 은 화풍만 다릅니다** (E 원두 고정). **S19 는 추가 요청에서 분위기 문장만 뺍니다**
(S14 와 비교). 타겟 문장은 남깁니다 - 통째로 비우면 되물음이 나서 대조가 깨집니다.

```
S19 추가 요청 : 집에서 커피를 내려 마시는 30대가 주 타겟입니다.
```

---

## 4. 만화형만 - 여섯 칸 보고 한 줄

| 회차 | 1번 칸 제품 + 주배경 | 앞 칸 사용 장면 (칸 번호) | 여섯 칸 포장 장소 일치 | 최장 대사 (자) | 오탈자 | 인물 유지 |
|---|---|---|---|---|---|---|
| *예시* | *O* | *O (3번)* | *X* | *22* | *없음* | *O* |
| S04 | O | X | O | 10 | 없음 | O |
| S05 | O | X | O | 10 | 없음 | O |
| S06 | O | O(1번) | **X** | 11 | 있음 | O |
| S07 | O | X | O | 14 | 없음 | O |
| S08 | O | X | O | 9 | 없음 | O |
| S09 | O | X | O | 10 | 없음 | X |
| S10 | O | X | O | 9 | 없음 | O |
| S11 | O | X | O | 11 | 없음 | O |

**2026-08-29 조치로 판정이 바뀌었습니다** (생성_파이프라인 4.2절). 제품은 1번 칸부터 여섯
칸 모두에 놓여 있는 것이 정상이므로, "앞 칸에 제품이 나왔는가" 로 적지 않습니다.

- **1번 칸 제품 + 주배경**: 1번 칸에 제품과 주배경이 함께 그려졌으면 O 입니다. 나머지 다섯
  칸이 이 칸만 레퍼런스로 보므로, 여기가 X 면 아래 두 열은 볼 것이 없습니다
- **앞 칸 사용 장면**: 1 ~ 3번 칸에서 인물이 제품을 들거나 꺼내거나 쓰고 있으면 O 이고 칸
  번호를 함께 적습니다. 놓여 있기만 한 것은 X 입니다 (정상). N26 이 묻는 축입니다
- **여섯 칸 포장 장소 일치**: 여섯 칸의 제품 포장 모양과 장소가 서로 같으면 O 입니다.
  조치 전에는 고정되지 않는 것이 알려진 한계였고, 이 열이 그것을 처음 재는 자리입니다

대사 25자 상한은 지침뿐이고 검사 코드가 없어 넘겨도 통과합니다 - 실제로 몇 자인지가
이 열의 전부입니다.

**S06 의 셋째 열이 X 인 근거**는 3절 메모입니다 - 1번 칸 불판에 있던 고기가 2번 칸에서
사라졌습니다. 제품이 한 칸에서 없어진 것은 "여섯 칸의 포장과 장소가 서로 같다" 가 아닙니다.

**S07 의 관측은 제품이 두 자리에 동시에 있는 것입니다** - 5, 6번 칸에서 인물이 모자를 썼는데
벤치에도 모자가 그대로 남아 있습니다. 인물과 배경이 각각은 유지됐으나 제품이 복제됐습니다.

**계획서 F-b 7(`note` 갈래별 반영도): 네 갈래 모두 반영됐습니다.**

| 회차 | 갈래 | `note` 반영 |
|---|---|---|
| S06 | 배경 지정 (가족이 둘러앉은 식탁) | O |
| S07 | 인물 지정 (20대 대학생) | O |
| S08 | 캐릭터 지정 (북극곰) | O |
| S09 | 상황 지정 (시험 기간에 집중) | O |

**칸 단위로는 세지 않았습니다.** 계획서 F-b 7 이 물은 "배경이 몇 번째 칸까지 유지되는가",
"북극곰이 여섯 칸 모두에 나오는가" 는 따로 기록하지 않았고, 위 표는 요청한 것이 결과에
나타났는지까지입니다.

**`Brief.character` 를 채우는 경로가 없는데도 S08 이 O 라는 것이 이 표의 값입니다.**
`brief:fill` 이 캐릭터를 추출하지 않고 화면에도 입력칸이 없으므로, 만화형에서 캐릭터를
지정하는 통로는 `note` 자유 텍스트 하나뿐입니다 (계획서 C-b-1절).

---

## 5. 가드레일 한 번

**화면에서 이 입력으로 시안까지만 만들어 봅니다.** `확정` 을 누르지 않으면 이미지 요금이
들지 않습니다.

```
제품명    : 아디다스 모자
제품 특징 : 멋진 모자
추가 요청 : 작년에 지역 박람회에서 상을 받았습니다
```

**계획서 D-d 의 조합과 다릅니다.** 계획은 `대나무 물티슈` / `무향 무알코올` 이었고 실제로
친 것은 위와 같습니다. 방아쇠인 "상을 받았습니다" 는 그대로라 조건이 사라지지는 않았지만,
**아래 재현율 7/12 는 물티슈 조합에서 잰 값이라 이 1건에 그대로 적용되지 않습니다.**

근거에는 "상을 받았습니다" 가 있는데 카피가 **"수상"** 이라고 쓰면 걸립니다.

| 나온 카피 | 거절됐나 | |
|---|---|---|
| 멋을 쓰다, 아디다스 모자. | 거절되지 않음 | |

거절 화면이 뜨면 캡처하십시오. 안 걸릴 수도 있습니다 (재현율 7/12).

**on/off 대조는 화면으로 못 합니다** - 운영 경로는 항상 on 입니다. 그 대조는
[가드레일 대조 실측 보고서](가드레일_대조_실측_보고서.md)에 이미 있습니다 (양 팔 30건씩).

**S01 ~ S19 카피에 이상한 문구가 있었나** (없는 수치, 타사 비교, 최상급, 수상):

---

## 6. 끝나고

**스크린샷을 자료에 넣을 때 주소창을 자르십시오.** 공개 호스트가 외부 IP 형태이고 저장소는
public 입니다.

`/srv/adcraft/app/infra/.env` 를 VS Code 에서 열고 두 줄을 되돌립니다.

- `ADGEN_GENERATION_MODE=model` -> `stub`
- `ADGEN_MODEL_API_KEY=...` 줄을 **줄째 삭제**

저장할 때 상태바가 **`LF`** 인지 봅니다. `CRLF` 면 ai-engine 이 기동하지 않습니다.

```bash
cd /srv/adcraft/app && bash scripts/deploy-vm.sh --no-build

C="docker compose -f infra/docker-compose.yml"
$C logs ai-engine | tail -20 | grep "generation seams"   # 'stub' mode
$C exec ai-engine sh -c 'echo keylen=${#ADGEN_MODEL_API_KEY}'   # 0
grep -rln 'sk-' infra/ 2>/dev/null                       # 아무것도 안 나와야 정상

# 백업 파일을 만들었다면 키가 평문으로 남아 있습니다
ls infra/.env.bak-* 2>/dev/null && shred -u infra/.env.bak-*
```

**VS Code 로 직접 고치면 백업이 안 생기므로 대개 지울 것이 없습니다.** 다만 `cp` 로 한 번이라도
떠 두었다면 **키가 들어간 뒤의 사본**일 수 있습니다. 2026-08-27 에 정확히 이 자리를 밟았고
사후에 `shred -u` 로 지웠습니다 (이슈 #291).

| [ ] 결과 이미지 전부 회차 번호로 이름 붙임 |
|---|
| [ ] `.env` 두 줄 되돌리고 재배포 |
| [ ] `'stub' mode` 와 `keylen=0` 확인 |
| [ ] **`.env.bak-*` 가 있으면 `shred -u` 로 삭제** |
| [ ] 디스코드 `!usage` 로 실제 집행액: ______ (예상 약 3.9) |
