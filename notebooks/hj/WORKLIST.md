# 03 AI 생성과 서빙 - 작업 목록 (신호정)

작성 2026-08-19. 개인 작업 목록입니다.

> 주의: **이 파일은 근거가 아닙니다.** 결정의 근거는
> [AGENTS.md](../../AGENTS.md) "근거 자료 규칙" 표를 따르며, 여기 적힌 것은 그 문서들에서
> 제 몫만 뽑아 온 사본입니다. 두 곳이 어긋나면 원본이 맞습니다.
>
> 원본:
> [03-AI_생성_서빙.md](../../docs/역할_일정/03-AI_생성_서빙.md) /
> [미결정_대장.md](../../docs/기술문서/미결정_대장.md) /
> [생성_파이프라인.md](../../docs/기술문서/생성_파이프라인.md) /
> [구현_범위.md](../../docs/공통_가이드/구현_범위.md) F11, F12 /
> [2026-08-19 회의록](../../docs/회의록/2026-08-19_소관배정_화풍확정_카피경로확인.md)

역할은 2026-08-14 재배정 기준 **02 테크리드 + 03 AI 생성과 서빙** 겸임이며, 아래는 **03 몫만**
추린 것입니다. 테크리드 업무(리뷰, 계약, ADR 작성)는 제외했습니다.

---

## 0. 품질 티어 운용 지침

**개발과 실험은 `low`(또는 스텁), 최종 확인만 `medium`.**

이것은 새 결정이 아니라 이미 문서에 있는 것을 작업 규칙으로 옮긴 것입니다 -
[생성_파이프라인.md](../../docs/기술문서/생성_파이프라인.md) 6.2절 마지막 문단이 같은 내용입니다.

| 상황 | 티어 | 이유 |
|---|---|---|
| 배관과 합성 로직 확인 | 스텁 모드 | 요금이 나가지 않습니다 |
| 컷별 생성, 병렬, 실패 처리 개발 | `low` | 세트당 0.0974 USD. 회차를 많이 태워도 예산이 버팁니다 |
| 검증 실험 (3순위, 5순위, N18) | `low` | 판정 대상이 화풍과 일관성이라 티어가 판정 축이 아닙니다 |
| 최종 확인, 시연, 제출용 산출물 | `medium` | 세트당 0.4041 USD |

주의 세 가지입니다.

- **운영값은 그대로 만화형 `medium`입니다.** 위 규칙은 개발 중 비용 방어이지 운영 설정값을
  바꾸는 것이 아닙니다. 단일 광고형은 개발이든 운영이든 `low` 고정입니다.
- **`low`로 잰 숫자를 `medium`의 근거로 쓰지 않습니다.** 시간과 단가가 티어마다 다르고,
  병렬 시 개별 호출 팽창률도 다릅니다(`low` 44%, `medium` 64%).
- **`low` 만화형에는 관측된 결함이 있습니다** (손과 사물의 물리). 개발 중 그림이 이상한 것은
  구현 결함이 아닐 수 있으니 `medium` 1회로 먼저 갈라내고 원인을 판단합니다.

**강제 지점은 `ADGEN_IMAGE_QUALITY_OVERRIDE` 입니다** (2026-08-20 구현). 운영 티어는 계약을
타고 오고(만화형 `medium` / 단일 광고형 `low`), 개발과 실험에서 `low` 로 내리는 것은 이 엔진 쪽
환경변수로 합니다. backend 의 `COMIC_QUALITY` 상수를 고쳐서 내리면 **그 값이 그대로 배포됩니다.**
override 가 발동하면 경고 로그가 남습니다.

---

## A. `image:render` 실물화 마무리 (2026-08-19 배정, 최우선)

**2026-08-20 에 컷별 생성으로 갈아끼웠습니다.**
[render.py](../../apps/ai-engine/src/ai_engine/render.py)의 `_render_panels` 가 칸을 하나씩
만들어 3x2 로 붙입니다. 한 장 통째 요청은 남겨 두지 않았습니다 - `render_prompt.build` 에
만화형을 넘기면 `TypeError` 입니다. ADR-0017 이 폐기한 방식이라 조용히 되돌아갈 자리를 두면
안 되기 때문입니다.

**실물 확인도 끝났습니다 (2026-08-20).** `low`, `medium` 각 1회를 `POST /v1/image:render` 로
돌려 규격과 시간을 픽셀과 시계로 쟀습니다. 회차 기록:
[API생성_시험_보고서.md](../../docs/보고서/API생성_시험_보고서.md) E-b-2 절.

⚠️ **`medium` 회차에서 장면 상태 연속성 문제가 재현됐습니다** - 6번 칸에 1번 칸의 쏟아진
커피가 다시 나타났습니다. 인물은 여섯 칸 내내 유지됐습니다. C2 의 판정 시트 수정이 필요한
근거가 하나 더 생긴 셈입니다.

- [~] **A4. 05(임동규)와 인터페이스 선합의** - **이슈 #141 로 올렸고 답 대기입니다**
  - 코드를 읽어 보니 **05 쪽에 새로 구현할 것이 없습니다.** 부분 실패(N20-a)는 지금
    이음매가 이미 잡 전체 `failed` 이고, 잡 직렬(N9)은 `jobs.next_queued` 가 저장소
    성질로 보장합니다. 필요한 것은 `render_timeout_s`(300초)를 내리지 않는 것뿐입니다
  - **한 가지 어긋난 것**: `render_timeout_s`(응답 전체)와 `image_timeout_s`(호출 하나)가
    둘 다 300초라, 컷별 최악 600초인데 backend 가 300초에 포기합니다. 그때 요금은
    나가고 렌더 1회(INV-3)도 소진된 채 실패로 뜹니다. 제안은 칸당 120 / 총 240 입니다
  - ⚠️ **2026-08-20 부터 이 어긋남이 실제 경로가 됐습니다.** A1 ~ A3 이 들어가면서 만화형
    렌더가 실제로 호출 6회를 냅니다. 관측치(102.6 ~ 110.8초)로는 여유가 크지만, 그것은
    최악값이 아니라 평균 쪽입니다. **두 값 다 이 이슈에서 함께 정합니다** - 엔진 쪽만 120으로
    내리면 아직 답이 없는 총 예산과 어긋난 채로 굳습니다
  - 동시성이 두 앱에 나뉩니다(N19-a). 잡 안의 칸 5장 동시 호출은 03, 잡끼리 몰리지 않게 막는
    것은 05입니다
  - 두 앱의 허용된 결합은 `packages/contracts`의 HTTP 계약뿐입니다. 구현을 먼저 시작하면 그
    선을 넘는 쪽이 쉬워집니다
- [x] **A1. 컷별 생성 + 3x2 합성 (2026-08-20 구현)**
  - 칸 크기는 `spec` 을 3x2 로 나눠 얻습니다. 1152 를 코드에 적지 않은 이유는 `ImageSpec` 을
    유도하지 않는 이유와 같습니다 (미결정_대장 N16)
  - 나누어떨어지지 않는 캔버스는 **호출 전에** 거절합니다. 그대로 보내면 요금이 나간 뒤에
    띠가 남습니다
  - 배치는 픽셀로 검사합니다. "6칸이 균등한가"를 Y/N 으로 보면 자리가 뒤바뀌어도 통과합니다
  - **DoD 닫힘 (2026-08-20).** `POST /v1/image:render` 로 두 회차를 실제로 돌렸습니다 -
    캔버스 3456 x 2304, 칸 1152 x 1152, 바깥 여백 0px. 여섯 칸이 다 요청한 크기로 와서
    합성의 크기 보정이 한 번도 발동하지 않았습니다. 대사는 두 회차 다 6/6 무오탈자
- [x] **A2. 컷 병렬 호출 (2026-08-20 구현)** - 1번 칸 뒤 다섯을 동시에 부릅니다
  - 병렬 여부를 **시간이 아니라 동시 도착으로** 검사합니다(`threading.Barrier`). `sleep` 으로
    재면 느린 CI 에서 흔들려 결국 아무도 믿지 않는 테스트가 됩니다
  - **DoD 닫힘 (2026-08-20).** 운영 경로 실측 `low` 52.8초 / `medium` 118.0초. 하네스 값
    (55.9 / 139.2초)보다 빠르고 `render_timeout_s`(300초) 안에 듭니다
  - 회차 기록은 [API생성_시험_보고서.md](../../docs/보고서/API생성_시험_보고서.md) E-b-2 절
- [x] **A3. 부분 실패 처리 (2026-08-20 구현, N20-a)**
  - 한 칸이라도 실패하면 `RenderFailedError` 로 세트를 버립니다. 몇 번 칸인지가 메시지에
    남습니다 - 호출이 6회라 그 정보 없이는 로그만 보고 재현할 수 없습니다
  - **계약은 바뀌지 않아 ADR을 새로 남기지 않았습니다.** `JobStatus` 에 부분 성공이 생기지
    않았고 잡을 `failed` 로 옮기는 것은 기존 경로 그대로입니다
- [x] **A5-0. 방식 결정 (2026-08-20)** - **요청이 티어를 실어 보냅니다.** 설정을 유형별로
      쪼개지 않습니다
  - 근거는 같은 계약 안의 선례입니다. `ImageSpec` 설명이 "**호출자가 규격을 정해 보냅니다.**
    엔진이 유형을 보고 스스로 정하면 기획서 10.2의 값이 두 곳에 생기고, 한쪽만 고치는 순간
    어긋납니다"라고 못박아 뒀고, 품질 티어도 출력 유형별로 갈리는 값이라 사정이 똑같습니다
  - 실제로 backend 에 이미 그 형태가 있습니다 -
    [render.py](../../apps/backend/src/backend_core/render.py) 의 `COMIC_SPEC` /
    `SINGLE_AD_SPEC` 상수와 `spec = COMIC_SPEC if session.output_type == "comic" else ...`.
    티어는 이 줄 옆에 상수 하나가 더 붙는 모양이 됩니다
  - 소관: 미결정_대장 E-2 의 이 행이 03 소관이라 회의록 없이 정할 수 있습니다. 다만 계약을
    바꾸므로 05 와 함께 봅니다 (A4 에 얹습니다)
- [x] **A5-1. 계약 먼저 (2026-08-20 완료)** - `ImageRenderRequest` 에 `quality` 추가
  - ⚠️ **먼저 답해야 하는 것 하나**: 요청만이 티어를 정하면 0절의 "개발 중에는 `low`" 지침을
    엔진 쪽에서 누를 수단이 없어집니다. backend 가 보낸 값이 곧 티어가 되기 때문입니다
  - **잠정안대로 갔습니다.** 계약의 `quality` 는 `spec` 과 같이 **필수**이고, 엔진에는 개발용
    강제 override 를 다른 이름으로 남겼습니다 (`ADGEN_IMAGE_QUALITY` ->
    `ADGEN_IMAGE_QUALITY_OVERRIDE`). 발동하면 경고 로그를 남깁니다
  - 벤더의 `auto` 는 열거에서 뺐습니다 - 모델에게 맡기는 것이 막으려던 바로 그 상태입니다.
    `high` 는 넣되 "재지 않았다" 를 계약 설명에 적었습니다
- [x] **A5-2. 스키마 (완료)** - 양쪽 모델에 반영
  (`backend_core.models.generation`, `ai_engine.models.generation`)
- [x] **A5-3. 구현 (완료)**
  - backend: `COMIC_SPEC` 옆에 `COMIC_QUALITY = "medium"` / `SINGLE_AD_QUALITY = "low"`
  - engine: `_render_with_model` 이 설정이 아니라 요청에서 티어를 읽음
- [x] **A5-4. 문서 (완료)** - 미결정_대장 E-2 해당 행, `config.py` 의 `image_quality` docstring,
  [생성_파이프라인.md](../../docs/기술문서/생성_파이프라인.md) 6.2절 마지막 문단
  - ADR 은 만들지 않는 쪽으로 봅니다. 기존 `spec` 패턴을 1:1로 따라가는 필드 추가이고, 새로
    가르는 갈래가 없습니다. 판단이 뒤집히면 여기서 되살립니다
- [x] **A6. 캐릭터 일관성 구현 (2026-08-20, A-4)** - 1번 칸 결과를 2 ~ 6번 칸 레퍼런스로 전달
  - A2 와 같은 시점에 나왔습니다. 레퍼런스가 1번 칸 하나라는 성질이 곧 병렬의 조건이라,
    둘을 따로 짤 수가 없습니다
  - 실험 하네스와 **한 곳 다릅니다**: 브리프의 `character` 를 1번 칸 프롬프트에 함께 보냅니다.
    1번 칸은 레퍼런스가 없는 유일한 칸이라 인물을 알려 줄 통로가 그 필드뿐입니다
  - ⚠️ **"일관성이 해결됐다"로 읽지 마세요.** 잡히는 것은 인물이고, **장면의 상태**는
    구조적으로 안 잡힙니다 (C2 항목). 대장 등급은 여전히 잠정 진행이며 확정은 검증 3순위 뒤입니다

## B. 남은 이음매 실물화

- [x] **B1. `brief:fill` 실물화 (PR #145 머지)** - 사진 판독 1회로 카테고리와 타겟이 채워짐
  - 실패 시 자동 채움만 건너뛰고 `messageMode: degraded`로 진행 (ADR-0005)
- [x] **B2. `draft:generate` 실물화 (PR #145 머지)** - 단일 광고형 시안이 실제 GPT API에서 나옴
  - 카피 경로는 2026-08-19 확인으로 기획서 13.2(텍스트와 이미지 모두 GPT API) 유지 확정
- [x] **B3. `draft:patch` 실물 분기 (PR #145 머지)** (`_patch_with_model`)
  - B2와 같은 모델 호출을 공유하므로 같은 시점에 처리합니다
- [ ] **B4. 대사 길이 상한 분기** - **선행 조건 해소됨.** C1 수치가 나왔습니다(아래)
  - 상한 초과 카피를 재생성할지 거절할지 함께 정합니다. 수치만 정하고 비워 두면 구현이 다시 멈춥니다

## C. 검증 실험 (수행은 03, 판정은 정승호, 임동규, 송기하)

- [x] **C1. N18 대사 길이 상한 시험 (2026-08-20 완료, PR #146)**
  - 운영 조건(컷별 생성, `medium`) 5세트에서 **15 ~ 25자 전부 무오탈자.** 단건도 두
    티어 전 길이 통과. 상한 25자를 막지 않지만 **26자 이상은 재지 않았습니다**
  - 첫 회차 13장은 한 장 6칸 프롬프트를 써서 무효 (ADR-0017 이후 운영 경로가 아님)
  - **회의가 확정할 것**: 상한 수치 + 초과 시 재생성이냐 거절이냐 (B4 가 여기 걸립니다)
  - 이 구간 표본이 0건이라 상한을 못 정하고 있습니다 (6 ~ 14자는 오탈자 0건, 27자는 1건)
  - B4의 선행 조건입니다
- [ ] **C2. 검증 3순위 캐릭터와 화풍 일관성** - 판정자 3명 중 2명 이상 동일 인물 판정
  - ⚠️ **착수 전에 판정 시트를 고쳐야 합니다.** C1 회차에서 판정자 두 분이 **장면 상태**
    연속성 문제를 발견했습니다(닦아낸 커피가 다시 쏟아짐, 책상 길이 변화). 인물은
    유지되는데 상태가 유지되지 않는 것이라 1번 칸 레퍼런스로는 구조적으로 안 잡힙니다
  - 지금 시트는 "같은 인물인가"와 "화풍이 일관되는가"만 묻습니다. **이대로 돌리면
    통과가 나오는데 결과물은 앞뒤가 맞지 않습니다.** 미결정_대장 A-4 에 적어 뒀습니다
- [ ] **C3. 검증 5순위 화풍 반영** - 화풍별 결과가 구분 가능
- [ ] **C4. 화풍 8종 예시 이미지 자체 생성** - 06(송기하)과 공동 소관
  - A-3(화풍 후보와 예시)의 남은 절반입니다. 목록은 2026-08-19에 확정됐습니다
  - 외부 이미지 금지(저장소가 public이라 커밋 순간 되돌릴 수 없습니다)
  - **예시와 실제 결과가 같은 프롬프트 조각에서 나와야** 합니다. 예시만 예쁘면 선택 화면 전체가
    거짓말이 됩니다

> 검증 결과 이미지는 그때그때 메신저에 올립니다. 판정자가 결과를 못 보면 교차 검증이 형식만
> 남습니다. 판정은 AI 담당(03)을 제외한 전원이 합니다.

## D. 가드레일 (F11, F12 - 아직 손대지 않음)

- [ ] **D1. 가드레일을 광고 경로로 이식**
  - `ai_engine.guardrail`의 유일한 라우트 수준 사용처가 아직 `/v1/generate`입니다.
    옮긴 뒤 그 라우트를 삭제합니다
- [ ] **D2. on/off 대조 실행**
  - DoD: 델타가 `eval/` 출력에 숫자로 남음. 거절 시 1회 재생성 후 `CONTENT_POLICY_REJECTED`
  - 가드레일 우회는 측정 자체를 무효로 만듭니다

## E. 문서

- [ ] **E1. 인프라 문서 보완** - 실제 사용 모델과 하드웨어 최적화 여부 (2026-08-11 회의 지정)
- [ ] **E2. 어댑터 ADR** - 조건부. A-5(로컬 모델 채택 여부)가 채택으로 닫힐 때만. 정승호 공동

---

## 제 몫이 아닌 것 (혼동 방지)

| 항목 | 소관 |
|---|---|
| 검증 4순위 로컬 모델과 GPT 비교 | 04 (정승호). 판정자는 임동규, 송기하 2명 |
| 잡 단위 동시 실행 상한 / 큐 처리 | 05 (임동규). `render_timeout_s`와 잡 워커가 `backend_core` 소유 |
| 기획서에 A-2 결정 반영 | 01 (정승호) |

## 착수 순서 (2026-08-20 재갱신)

**A 덩어리는 코드로는 끝났습니다** (A1, A2, A3, A5, A6). B는 #145, C1은 #146으로 끝났습니다.
남은 것은 전부 **다른 사람의 답이나 실측**을 기다리는 항목입니다.

1. ~~실물 1회 확인~~ **끝났습니다 (2026-08-20).** `low` 1세트 + `medium` 1세트를
   `image:render` 로 돌렸습니다. 남은 만화형 `medium` 예산은 약 48세트입니다.
2. **A4(이슈 #141)가 이제 실제 경로를 막습니다.** 만화형이 호출 6회를 내기 시작했으므로
   타임아웃 예산 어긋남이 가설이 아니라 현재 설정입니다. 엔진 쪽만 고치지 말고 두 값을 함께
   정합니다.
3. **C2는 판정 시트 수정이 먼저입니다.** 장면 상태 축을 묻지 않는 시트로 돌리면 통과가
   나오는데 결과물은 앞뒤가 맞지 않습니다.
4. **B4는 회의의 상한 확정을 기다립니다.** 수치와 초과 처리(재생성이냐 거절이냐)가 함께
   정해져야 구현이 한 번에 끝납니다.
5. **D(가드레일)는 위 넷과 독립입니다.** 기다릴 것이 없으므로 대기가 길어지면 여기를 잡습니다.

**`run_panels.py`가 A1 ~ A3, A6의 참고 구현이었습니다.** 호출 모양(`generate` 1회 +
`edit` 5회 동시, 3x2 합성)을 그대로 가져왔으므로 C1의 **7세트(42회 호출)** 실측치가 이전됩니다.
바꾼 곳은 두 군데이고 둘 다 이유가 있습니다 - 브리프의 `character` 를 1번 칸에 함께 보내고
(하네스는 시나리오가 고정이었음), 레퍼런스를 파일 객체가 아니라 바이트로 넘깁니다(다섯
스레드가 파일 하나를 함께 읽으면 읽기 위치가 섞임 - 순차인 하네스에는 없던 함정).

## 제약

- **날짜를 채울 수 없습니다.** B-20(단계별 마일스톤 일자)이 차단 상태라 08-17 이후 날짜가 전부
  테크리드 초안입니다. 위 목록은 순서만 확정이고 기한은 회의록 사안입니다.
- **예산이 실질 제약입니다.** 남은 약 24 USD 중 만화형 몫이 20 USD이고 `medium` 세트당
  0.4041 USD이므로 **약 49세트가 상한**입니다. 0절 지침이 이 상한을 지키기 위한 것입니다.
