# 아키텍처

> TODO: 프로젝트 내용으로 채우세요. 아래 4절(의존 방향)과 5절(이음매)은 템플릿이 코드로 이미
> 강제하고 있는 부분이라 **그대로 유지**하기를 권합니다. 1~3절만 교체하면 됩니다.

## 1. 전체 그림

> TODO: 다이어그램 또는 구성 요소 목록. 외부 의존(모델 API, 벡터 DB, 외부 서비스)을 반드시
> 명시하세요 — 나중에 "무엇을 스텁으로 두었는지" 추적하는 기준이 됩니다.

```
[클라이언트] → [backend] --HTTP--> [ai-engine] → [벡터 DB / 모델 API]
                   │
                   └→ [외부 API · 저장소]
```

## 2. 모듈

| 모듈 | 책임 | 위치 |
|---|---|---|
| backend | 요청 수신, 도메인 처리, 오케스트레이션, 폴백 | `apps/backend/` |
| ai-engine | 근거 검색 + 가드레일 생성 (독립 배포) | `apps/ai-engine/` |
| frontend | 사용자 인터페이스 | `apps/frontend/` (미구현) |
| contracts | 모듈 간 HTTP 계약 | `packages/contracts/` |

## 3. 기술 스택

> TODO: 계층별로 채우세요. 선택의 **이유**가 자명하지 않은 항목은 ADR로 남기세요.

| 계층 | 기술 |
|---|---|
| API | FastAPI / Python 3.12 |
| 검색 | TODO (벡터 DB) |
| 생성 | TODO (모델) |
| 인프라 | docker-compose / TODO |

## 4. 의존 방향 (위반은 스타일이 아니라 설계 결함)

```
apps/backend/src/api  ──의존──>  apps/backend/src/backend_core
apps/backend          ──HTTP만──> apps/ai-engine     (계약: packages/contracts)
```

- **`backend_core`는 `api`를 import하지 않습니다.** 도메인 로직이 FastAPI 없이 실행·테스트
  가능해야 평가 하네스와 오프라인 도구가 그 로직을 직접 부를 수 있습니다.
- **`ai-engine`은 `backend`를 import하지 않습니다(역도 마찬가지).** 두 앱은 별개 배포 단위이고,
  허용된 결합은 HTTP 계약뿐입니다. 이 선을 넘는 파이썬 import 한 줄이 "독립 배포 가능한 AI 모듈"
  이라는 성질을 조용히 없앱니다.
- **라우터는 얇게**: 요청 검증 → 도메인 호출 → 응답 매핑. 라우터 안의 비즈니스 로직은 결함입니다.
- **AI 엔진 내부 파이프라인은 단방향**: 파싱 → 청킹 → 임베딩 → 검색 → 생성. 역방향 의존 금지.

## 5. 이음매 (스텁 → 실물 교체 지점)

워킹 스켈레톤의 외부 의존은 전부 스텁이고, 각 스텁에는 **이름이 붙은 이음매**가 있습니다.
교체는 그 이음매에서 하세요 — 우회해서 옆에 새 경로를 만들면 스켈레톤이 검증하던 성질이 사라집니다.

| 이음매 | 현재 | 교체 후 | 소관 |
|---|---|---|---|
| `ai_engine.retrieval.Retriever` | 번들 픽스처 문자열 매칭 | 벡터 DB + 임베딩 | TODO |
| `ai_engine.generation.Generator` | `StubGenerator` (근거 되돌려주기) | 모델 클라이언트 | TODO |
| `api.deps.ai_client` | HTTP 클라이언트 (실물) | — | TODO |
| `backend_core.pipeline.FALLBACK_TEXT` | 자리표시자 문안 | 승인된 문안 | TODO |

## 6. 결정 기록

구조에 관한 결정은 [docs/adr/](../adr/)에 ADR로 남깁니다. 문서화되지 않은 결정은 결정한 사람이
자리를 비우는 순간 사라집니다.
