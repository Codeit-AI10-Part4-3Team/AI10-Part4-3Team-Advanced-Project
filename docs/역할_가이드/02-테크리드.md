# 02. 테크리드 — 신호정

> 일정은 [역할_일정/02-테크리드.md](../역할_일정/02-테크리드.md)에 있습니다.
> 배정을 바꾸면 이 제목과 [README.md](README.md)의 표, `CODEOWNERS`,
> [개발자_가이드.md](../공통_가이드/개발자_가이드.md) 5절을 **같은 PR에서** 함께 고치세요
> (2026-08-10 배정 완료).

## 한 줄 정의

이 역할이 없으면 **계약이 구두 합의로 남고**, 프론트·백엔드·AI가 서로 다른 것을 만들고 있다는 사실을 통합 시점에 발견합니다.

## 소유 경로

`CODEOWNERS`와 **같은 경로**여야 합니다. 다르면 리뷰 자동 배정이 엉뚱한 사람에게 갑니다.

- `packages/contracts/` (OpenAPI 단일 원천)
- `docs/adr/`
- `.github/` (CI·템플릿·라벨)
- `scripts/`
- 루트 `pyproject.toml`, `.pre-commit-config.yaml`

## 책임

- `openapi.yaml`을 광고 생성 계약으로 교체하고 **계약 동결일에 동결한다**(마일스톤 `08-08`).
  진행 상태와 지연 여부는 여기에 적지 않습니다 — 정본은
  [미결정_대장.md](../기술문서/미결정_대장.md)와 [리스크.md](../공통_가이드/리스크.md)입니다
- **워킹 스켈레톤의 더미 구현 3곳(S3 브리프 채우기 · S4 시안 생성 · S6 렌더)을 직접 만든다** -
  2026-08-11 회의 확정. AI 담당이 배관에 묶이지 않게 하려는 배치입니다
  ([구현_범위.md](../공통_가이드/구현_범위.md) 1.1절).
  더미 전용 경로를 새로 파지 않고 **실물과 같은 함수 안에서 분기**시키며, 분기 조건은 설정으로
  드러냅니다 — 어느 쪽이 돌고 있는지 코드를 읽지 않고 알 수 있어야 스텁 응답을 실측값으로
  착각하는 사고가 막힙니다
- 남은 결정이 기한 내 정해지게 몰아붙인다. **저장 위치는 2026-08-09에 닫혔습니다**
  ([ADR-0010](../adr/0010-상태_저장소와_파일_보관_위치.md)) - 남은 것은 이미지 생성 경로(ADR
  `0003` 예약), 파인튜닝 보류(ADR `0004` 예약), 비동기 잡 처리 방식입니다
- required status check 계약(`ci.yml` 잡 이름 ↔ `setup-github.sh` contexts)을 유지한다
- ruff 버전 4곳 동기화가 깨지지 않게 한다 (`ruff-sync` 훅이 강제하지만 상향 PR은 사람이 합침)
- 앱/폴더 추가 시 **네 곳**(루트 ruff `src` · `ci.yml` matrix · `setup-github.sh` contexts · `CODEOWNERS`)을 같은 PR에서 고치게 한다

## 하지 않는 일 (경계)

책임보다 이 항목이 분쟁을 더 많이 막습니다.

- 각 앱의 내부 구현 (라우터·도메인 로직은 소유자에게)
- 학습 하이퍼파라미터 (04 소관)
- 품질 게이트를 통과시키기 위해 게이트를 낮추는 판단 — 그것은 측정 포기입니다

## 산출물

| 산출물 | 위치 | 완료 판단 기준 |
|---|---|---|
| 동결된 계약 | `packages/contracts/openapi.yaml` | `contracts-check` 통과 + 양쪽 앱의 계약 준수 테스트 통과 |
| 더미 구현 3곳 | `ai_engine` (S3·S4·S6 이음매) | 실물과 같은 함수에서 분기하고, 분기 상태가 설정으로 드러남 |
| 결정 기록 | `docs/adr/000N-*.md` | 미결정 표(ADR README)가 비워짐 |
| 녹색 CI | `.github/workflows/` | main에서 required 5종이 모두 통과 |

## 인수인계 시 넘겨야 하는 것

이 역할이 빠졌을 때 다음 사람이 곧바로 이어받으려면 무엇이 문서로 남아 있어야 하는가.
**"머릿속에만 있는 것"을 여기에 적고, 그것을 없애는 것**이 이 항목의 목적입니다.

- 계약에서 **의도적으로 열어 둔 필드**와 그 이유 (optional로 둔 것은 미정의 흔적)
- CI가 특정 방식으로 짜인 이유 중 주석에 없는 것
- 브랜치 보호 설정의 현재 값 (GitHub 서버에만 있어 diff에 보이지 않습니다)
