# 01. 기획/총괄 (PM) — 정승호

> 일정은 [역할_일정/01-기획_총괄.md](../역할_일정/01-기획_총괄.md)에 있습니다.
> 배정을 바꾸면 이 제목과 [README.md](README.md)의 표, `CODEOWNERS`,
> [개발자_가이드.md](../공통_가이드/개발자_가이드.md) 5절을 **같은 PR에서** 함께 고치세요
> (2026-08-10 배정 완료).

## 한 줄 정의

이 역할이 없으면 **범위가 조용히 늘어나고**, 22일이 지난 뒤에야 아무것도 완성되지 않았다는 것을 알게 됩니다.

## 소유 경로

`CODEOWNERS`와 **같은 경로**여야 합니다. 다르면 리뷰 자동 배정이 엉뚱한 사람에게 갑니다.

- `*` (CODEOWNERS 기본값 — 테크리드와 공동. 아래 규칙에 걸리지 않는 모든 경로)
- `docs/adr/` (테크리드와 공동)
- 주의: `docs/기술문서/`는 이 역할이 오너가 **아닙니다** — TL + 백엔드입니다.
  `API_계약.md`를 그대로 구현하는 사람이 리뷰에서 빠지지 않게 하려는 배치입니다

## 책임

- 구현 범위를 확정하고 `docs/공통_가이드/구현_범위.md`를 원천으로 유지한다 — 스켈레톤 항목
  S0 ~ S8의 **모든 행에 DoD와 소관이 채워진 상태**를 유지하는 것이 기준입니다
- **마일스톤 일자를 확정한다** — 차단 항목이며 이 역할이 판정자입니다
  ([미결정_대장.md](../기술문서/미결정_대장.md) B-20). 만드는 방법은 2026-08-11에 정해졌습니다:
  **AI 초안 후 이의 수렴**. 초안은 [역할_일정/](../역할_일정/README.md)에 있고 **아직 초안이므로
  지연 판정의 근거로 쓰지 마세요.** 확정은 회의록으로만 합니다 (기획서 14.3)
- 페이즈 전환을 **날짜가 아니라 DoD 충족**으로 판정한다
- 거절 문구와 면책 고지 등 **사용자에게 보이는 확정 텍스트**를 승인한다
  (폴백 문안은 없습니다. [ADR-0005](../adr/0005-열화_폴백은_자동화_생략으로_한정.md))
- 리스크 대장(`docs/공통_가이드/리스크.md`)의 감지 신호를 주 1회 확인한다
- 협업 일지(Discussions `CollaborationLog`)에 결정과 삽질을 남기게 하고, 비면 채운다
- 겸임 배정이 금지 조합에 걸리지 않는지 확인한다

## 하지 않는 일 (경계)

책임보다 이 항목이 분쟁을 더 많이 막습니다.

- 기술 선택 자체 (base 모델·큐·저장소 결정은 02 테크리드 소관, 근거는 ADR로)
- 코드 리뷰의 기술적 판단 (범위 이탈 여부만 본다)
- 지표 목표치를 실측 전에 정하는 일 — 목표치는 실측이 나온 뒤 채운다

## 산출물

| 산출물 | 위치 | 완료 판단 기준 |
|---|---|---|
| 확정된 구현 범위 | `docs/공통_가이드/구현_범위.md` | 표의 모든 행에 DoD와 소관이 채워짐 |
| 마일스톤·페이즈 | `docs/역할_일정/00-overall.md` | 절대 날짜 + 페이즈별 DoD. **회의록으로 확정될 때까지는 초안** |
| 면책 고지 문구 | 결과 화면과 다운로드 안내 | 문구가 확정되어 화면에 붙음 (미결정_대장 A절 7번) |
| 주간 협업 일지 | Discussions `CollaborationLog` | 주 1회 이상 게시 |

## 인수인계 시 넘겨야 하는 것

이 역할이 빠졌을 때 다음 사람이 곧바로 이어받으려면 무엇이 문서로 남아 있어야 하는가.
**"머릿속에만 있는 것"을 여기에 적고, 그것을 없애는 것**이 이 항목의 목적입니다.

- 범위에서 **뺀 것**과 그 이유 (넣은 것보다 이게 더 자주 잊힙니다)
- 외부 이해관계자(멘토·평가자)와의 합의 내용 중 문서에 없는 것
- 미해결 리스크의 현재 상태와 다음 판단 시점
